# Handoff: midi-boot-retry — implement issue 01

Written 2026-07-05 by the diagnosing session (opencode).

## Task

Implement `.scratch/midi-boot-retry/issues/01-retry-midi-access-at-boot.md`
— it carries the full diagnosis and acceptance criteria. One small change
in one file plus verification.

## Context you'd otherwise have to rediscover

- The adapter: `frontend/src/midi/adapter.ts` — `attachMidiController()`,
  the one-shot `navigator.requestMIDIAccess()` at the bottom is the bug
  site. Note the `disposed` flag contract and the doc comment in
  `frontend/src/components/MidiControllerBridge.tsx`: the bridge mounts
  once and never re-attaches ("re-attaching would re-request MIDI access")
  — which is exactly why the adapter itself must retry.
- Diagnosis evidence (2026-07-05, Electron shell, Inpulse 300 Mk2): device
  visible to the page as `connected/closed`, direct `onmidimessage` worked,
  connection store empty, replug no-op, reload fixed. So the boot request
  rejected once and everything downstream was fine.
- ADR 0002 house style: no Web MIDI mocking — the issue's Testing Decisions
  spell out what that means for verification here. Don't add a mock-based
  test.

## Workspace

Set up a FRESH lane per `docs/agents/parallel-work.md` (new jj workspace
off `main`, register in `/Users/murtaza/manadj/.lanes/`, pick an unused
port offset; a sandbox DB is probably unnecessary — this is a frontend-only
change verified by build/tests, but clone one if you boot the lane app).
Do not touch lanes `minimap-clarity`, `looping`, `setlist`, `setsugg`,
`sets`, or `analysis`.

## Landing

Review-gated (the issue explains why despite bugfix category): flip to
`ready-for-human` with a smoke-test walkthrough (relaunch shell, badge
green, pads work), toast the human, land only on approval.

## Suggested skills

- `implement`
