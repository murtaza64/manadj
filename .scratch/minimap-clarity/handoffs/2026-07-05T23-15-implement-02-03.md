# Handoff: minimap-clarity — implement issues 02-03 (port the prototype verdict)

Written 2026-07-05 by the grilling/prototype session (opencode).

## State

- Feature docs: `.scratch/minimap-clarity/PRD.md` — read the **Decisions**
  and **Verdict** sections first; they are the contract.
- Issues to implement, in order: `.scratch/minimap-clarity/issues/02-zoned-marks-and-played-dim.md`,
  then `.scratch/minimap-clarity/issues/03-play-guide-zoned-marks.md`.
  Issue 01 (the prototype) is done.
- All docs above are landed on `main`.

## Workspace: take over lane `minimap-clarity` (do NOT create a new lane)

- Workspace: `/Users/murtaza/manadj-minimap-clarity` (port offset +100:
  backend 8100, vite 5273; sandbox DB already cloned).
- Update `.lanes/minimap-clarity.md` (in the MAIN repo root,
  `/Users/murtaza/manadj/.lanes/`) with your session id and claimed areas
  when you start.
- The lane's `@` sits on change **oyrprrqt** — the throwaway prototype lab
  (`?view=minimap-lab`). It NEVER lands on main (prototype policy,
  `docs/agents/parallel-work.md`). Start your implementation as a fresh
  change off trunk: `jj new main -m "minimap-clarity: 02-zoned-marks-and-played-dim"`.
  Keep oyrprrqt around as the visual reference while you work; **abandon it**
  (`jj abandon oyrprrqt`) after both issues are approved and landed.
- A lane app may still be running (`uv run scripts/agent/lane_app.py`);
  restart it if the working copy moved under it.

## Where the code lives (explored, current as of today)

- Issue 02: `frontend/src/waveform/WaveformRendererV2.ts` — minimap-mode
  branches (`this.isMinimap`); marks are the overlay geometry pass
  (`pushCuePoint`, `pushHotCues`, `pushPlayhead`, `pushRect`), body dim is
  currently a ×0.55 brightness in the constructor. The played-dim wash can
  be a translucent black rect pushed between body and marks.
- Issue 03: `frontend/src/performance/PlayGuideMinimapMarks.tsx` +
  `frontend/src/performance/PlayGuideOverlay.css` (DOM ticks); guide data
  from `usePlayGuides` / `playGuideModel.ts` (`aTime`, `missed` — already
  computed).
- Visual reference: the lab page at `?view=minimap-lab` (on change
  oyrprrqt) and `frontend/src/prototype/minimap/variants.ts` (the `zoned`
  variant). Reference only — write fresh production code; do not import
  from `prototype/`.

## Verification and landing

- Per the issues' acceptance criteria. Agent-side: `npm --prefix frontend
  run build`, `npx vitest run` in `frontend/`; visual check via the lane
  app (Performance view with a deck pair that has saved transitions —
  track 9 has hotcues + transitions in the sandbox DB).
- This is feature work → **review-gated**: flip each issue to
  `ready-for-human` with a verification walkthrough, toast the human, and
  only advance `main` on verbal approval (`docs/agents/parallel-work.md`).

## Suggested skills

- `implement` — the main loop for working the issues.
- `code-review` — self-review before requesting human review.

## Out of scope

- Loop-region rendering (owned by `.scratch/looping/` issue 05; the visual
  verdict was handed to them as a comment).
- The editor GlobalMinimap.
- Any change to full-waveform (non-minimap) rendering.
