# Handoff: sets — implement issue 22 (transport polish + row vocabulary alignment)

## Task

Implement `.scratch/sets/issues/22-conductor-transport-ui-polish.md` INCLUDING its Scope addendum (added 2026-07-05 — read the whole issue; the addendum exists because the rows bundle you're building on predates the vocabulary it must now follow). Gate, `code-review`, **park ready-for-human** with the walkthrough printed in your output — this is visual work; expect an iteration round with the human.

## Workspace — handed over (idle lane setui)

- Workspace: `/Users/murtaza/manadj-setui` — the lane that just landed the rows bundle (23/18/20), so the row DOM you're restyling is this lane's own fresh work; read those Done comments to re-orient
- Registry: `.lanes/setui.md` — update Agent/Status/claims when you start
- Ports: +130 (backend 8130, vite 5303); sandbox DB fine as-is (styling work)
- One jj change: `sets: 22-conductor-transport-ui-polish`

## The vocabulary (perf-layout 08, LANDED — study it first)

Read `.scratch/perf-layout/issues/08-interaction-vocabulary.md` and its implementing change (`lpsvkryx`): hover = border glow (`box-shadow: 0 0 5px` via `currentColor`), press = fill (`--surface1`), on-state = color no glow. No scale effects, no hue-shifts, no bespoke hovers. Its issue comments flag `SetsSidebarSection.tsx` as still-unmigrated — that's in your scope per the addendum. Shared button classes: `.btn` family in `frontend/src/styles/utilities.css`. Bright saturated colors per AGENTS.md.

## Decisions already settled (do not re-litigate)

- Three-zone Set pane header: name/chips left, transport center, Auto-fill/Suggest right; no floating bar
- Transport never shrinks at narrow widths; secondary cluster wraps/collapses first
- Pick up lit/unlit + teaching tooltip survives, expressed in the vocabulary (lit = color on-state)
- Selection (18) and conducting-row highlights are STATES (color), not hovers — keep both simultaneously legible under the new hover treatment

## PARALLEL LANE STATE

- Lane `setlist` is running issue 25 (dual-driver bugfix) — Conductor/editor lifecycle files, not yours. Transport BEHAVIOR is untouched by you (styling only); if 25 lands mid-session, rebase is trivial.
- perfui lane is open but paused — its shared CSS (Player.css etc.) is landed; build on it, don't fork it. If you find a vocabulary gap that needs a base-class change, comment on perf-layout 08 rather than diverging locally.

## Gate

`uv run -m pytest`; `npm --prefix frontend run build`; `npx vitest run`; `uv run alembic heads` → one (no migration); lint on touched files.

## Suggested skills

- `implement`, `code-review` before parking

## Out of scope

Any behavior change (transport, pickup predicate, selection semantics); issues 24/25/26; base vocabulary changes (comment, don't fork).
