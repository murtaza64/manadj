# Handoff: editclaim — sets 37 edit click-through lazy claim

Written 2026-07-06 by the orchestrator. You are a fresh implementation session
for the **sets** feature in the manadj repo (jj, NOT git). One issue, one new
lane.

## Task

`.scratch/sets/issues/37-edit-clickthrough-lazy-claim.md` — opening a
transition from a set row must not silence a conducting set: session-only
open, deferred deck claim/loads, one-press audition arm. Grilled 2026-07-06;
the Decisions section is settled — do not re-litigate. Both former blockers
(24, 28) are landed; you branch off current `main`.
**Feature → park `ready-for-human`.**

## Read first

1. `docs/agents/parallel-work.md` — lanes, merge-based landing, review gate,
   docs fast-path, hotspot protocol. Binding.
2. `docs/adr/0026-parallel-process-v2-review-gate-merge-landing.md`.
3. `docs/adr/0022*` and `docs/adr/0024*` (shared decks; Audible surfaces /
   arbiter) — 37 is entirely about this claim model. Also sets 21's Done
   comment (`.scratch/sets/issues/21-*.md`) for the lazy-claim precedent
   you are extending, and 28's takeover rule you must compose with.
4. `.scratch/sets/PRD.md` + `CONTEXT.md` glossary.
5. The issue file. Acceptance = the Decisions section; the 2026-07-06
   datapoint at the bottom is the live repro.

## Workspace

Fresh lane `editclaim`:

- `jj workspace add --name editclaim -r main ../manadj-editclaim`
  (run from /Users/murtaza/manadj)
- Create `/Users/murtaza/manadj/.lanes/editclaim.md` (untracked registry):
  workspace path, your session id as `owner:`, feature `.scratch/sets`,
  claimed areas (TransitionEditor open/claim/audition plumbing —
  `openPair`/`openTake`, `ensureEditorAudible` call sites, audition
  arm/pending-play; editor waveform-strip data source), **port offset +240**
  (backend 8240, vite 5413), status line.
- Sandbox DB: `cp -c /Users/murtaza/manadj/data/library.db data/library.db`
  from your lane dir. Default workspace is READ-ONLY for you (`.lanes/`
  excepted).

## PARALLEL LANE FENCES

- **laneux lane is LIVE** (mix-editor 32, lane-UI/UX pass): it owns the
  editor's lane-timeline UI/visuals. You own open/claim/load/audition
  plumbing and the strip DATA source. Do not restyle or reshape lane UI;
  if a shared file is unavoidable, additive-append and leave a tracker
  comment on 37 listing exactly which editor files you touched. Check
  `.lanes/laneux.md` before starting and at issue boundaries.
- **setrows lane is PARKED with unlanded work** (26+36; may amend in
  place): owns SetDetailPane rows/header and the adjacency resolution
  seam. The set-row click-through entry point you change lives there —
  touch only the openPair/openTake side, not row rendering.
- Arbiter/audibleSurface and Conductor.ts are landed and unclaimed; the
  conductor lane is idle. Still: no reshaping of Conductor internals —
  37 changes the EDITOR side of the claim handshake.
- Tracker is append-only across lanes; status-line edits only on 37.
- Before filing any new issue, check the highest number at `main`
  (currently 42) — five collisions so far.

## Gate

- `uv run -m pytest`; `npm --prefix frontend run build`; `npx vitest run`
  in `frontend/`; `uv run --extra dev ruff check` on touched files.
- HARD invariant: `uv run alembic heads` → one head (no migration expected).
- The claim-before-load invariant and the audition-arm cancellation rules
  are pure seams — test them (pending-play cancels on any other transport
  gesture or displacement; precedent: `pendingCues` in SetDetailPane).

## Review park

- Lane app: `uv run scripts/agent/lane_app.py start` (vite
  http://localhost:5413).
- Flip 37 `ready-for-human` with a verification-walkthrough comment; land
  via docs fast-path. **Print the full walkthrough in your output message**
  — the human reviews from your transcript. The headline check: set
  playing → click edit on an adjacency → music keeps playing, session
  opens, strips show the OPENED pair → press play → set stands down, pair
  loads, audition starts when ready. Include the sounding-adjacency free
  case (no-op adopt) and the strip-fallback state if you hit the
  deck-coupling fallback.

## Suggested skills

- `implement`; `tdd` (arm/cancel state machine first); `diagnosing-bugs`
  if the strip decoupling fights back

## Out of scope

- Lane-timeline UI/UX (laneux's 32)
- SetDetailPane rows/header (setrows' 26+36)
- Conductor semantics, transport routing (34/38 landed — read-only)
- The strip-decoupling deep-dive if the fallback fires — file it, per the
  Decisions fallback clause

## No secrets

Nothing sensitive here or in referenced files; SoundCloud OAuth token in
backend config is untouched.
