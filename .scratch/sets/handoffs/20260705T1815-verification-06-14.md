# Verification note: sets 06 + 14 (setlist lane, 2026-07-05)

Both issues implemented and **parked ready-for-human** as the stack
`main → oxwwnsvl (06) → pkwxstws (14)` in workspace
`/Users/murtaza/manadj-setlist`. Lane app :5253 serves the stack;
review walkthroughs are in each issue's Comments. Feature work — do not
land without verbal approval. Issue 16 (pickup) queues on top of this
stack.

## What landed where

**06 — Tempo policies (`oxwwnsvl`)**
- Planner (`sets/planner.ts`): `PlanInput.tempo` (Riding | Fixed).
  Riding: Tempo return ramps (`tempoReturnEndSec` per windowed
  adjacency; quadratic track-time through the ramp; runway constraint
  `dMax = 2(nextBoundary − b)/(1+r)` clamps-faster + `insufficient-runway`
  flag). Fixed: per-entry deck rates `setTempo/bpm` (clamped ±25% with
  flags), windows rate-scaled via `rateOutgoing`/`authoredLocalAt`,
  tempo-match flag moot. Warnings restructured to `PlanWarning`
  (severity/kind/message/adjacencyIndex|entryIndex).
- Persistence: `sets.tempo_policy` + `sets.set_tempo_bpm`, migration
  `0021_oxwwnsvl` (run against the sandbox clone only), PATCH schema +
  router tests.
- UI: toolbar tempo chip (popover: policy switch, editable Set tempo
  seeded from track 1), warning chips on adjacency rows + toolbar ⚠
  count + ladder ⚠ markers and magenta Tempo-return strips.
- Conductor: zero changes needed (pitch flows through `planStateAt`).
  Key Lock: inherited per Deck already; nothing added.

**14 — Grace fade / load latency (`pkwxstws`)**
- Planner transform `applyGraceFades`: colliding windows truncate the
  deck occupant `grace` (5s) early with a synthesized ~2s fade replacing
  the authored tail (`PlannedEntry.graceFade`); hard-cut outgoings get
  the same grace with the cut instant unshifted (planned silence);
  degenerate pileups plan as-authored + `grace-floor` ERROR flag;
  subsumes the raw window-overlap warning. Authored windows never move.
- ADR: `docs/adr/0025-grace-fade-rewrites-the-plan-tail.md`.
- Conductor: clock freezes only when NOTHING is audible (never stall
  while music plays / never skip while silent; late deck joins at plan
  position via the existing per-tick sync); `prefetchAhead` warms the
  decode cache one entry past the deck occupants (`sets/prefetch.ts`,
  new `Mixer.audioContext()` accessor).
- UI: ladder draws the fade wedge + hatched dropped tail; row chips
  "overlap: previous track fades early" / red "overlap pileup".

## Verification run (both changes, re-run after rebase)

- `uv run -m pytest` — 651 passed.
- `npm --prefix frontend run build` — tsc + vite clean.
- `npx vitest run` — 944 passed (planner suite: 43, includes 12 new
  tempo tests + 6 new grace-fade tests).
- `uv run alembic heads` — exactly one (`0021_oxwwnsvl`).
- ruff clean on touched python.
- Sandbox smoke: migration auto-applied on lane-app restart; tempo
  fields round-trip over the API (PATCH fixed/128 → riding/null).
- `code-review` ran per issue (standards + spec sub-agents). 06: fixed
  duplicate default constant, extracted `WarningChip`, removed the
  Fixed-without-BPM ramp leak. 14: replaced the
  `mixer.portFor('A').ensureAudio()` chain with `Mixer.audioContext()`.
  No bugs found by either review after fixes.

## Rebases

Trunk moved twice during the session; stack rebased onto trunk tip at
the 14 boundary (clean, no conflicts) and fully re-gated. If trunk moves
again before approval, re-rebase + re-run `sets/planner.test.ts` (the
looping stack touches `vectorize.ts`/`mixModel.ts`).

## Known gaps / follow-ups (non-blocking, noted in issue comments)

- Tunable heuristics (`tempoReturnSecPerPercent`, `graceHeadroomSec`,
  `graceFadeSec`) live in `sets/setSettings.ts` (localStorage) with no
  edit UI yet — matches the follow-params precedent but "settings"
  implies reachable; candidate ticket.
- A failed prefetch isn't retried for that entry (degrades to a slow
  load; the grace headroom still covers it).
- Conductor freeze check uses plan-`playing && ready`, not audibility: a
  deck plan-playing at fader 0 counts as "sounding" (late-join instead
  of hold). Edge case, flagged in review as minor.
- Entry-level warnings (`no-bpm`, `pitch-clamped`) surface only in the
  toolbar aggregate, not on track rows.

## Approval mechanics

On "approved, land it": `jj bookmark move main --to pkwxstws` (or to
`oxwwnsvl` for a 06-only prefix), then `jj new` off the old head, check
default@ per docs/agents/parallel-work.md (post-landing hot-reload —
moving default@ onto the new trunk auto-migrates the REAL DB with
0021 on next backend start; that is the sanctioned path). Review fixes:
amend into the parked changes (this lane's right).
