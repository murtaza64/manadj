# Handoff: sets — implement issues 06 + 14 (planner track)

## Task

Implement, in order:

1. `.scratch/sets/issues/06-tempo-policies.md` (Riding ramps + Fixed set tempo)
2. `.scratch/sets/issues/14-grace-fade-overlap-load-latency.md` (grace fade: truncation transform, freeze→late-join split, prefetch, ladder surfacing)

Gate each; **park the stack ready-for-human** with review-request comments and walkthroughs (feature work never advances `main` without the human's verbal approval). After 14 is parked — or earlier if you hit a wall — stop and write a verification note. Issue 16 (pickup) is queued behind you — do NOT start it, but keep `planStateAt` clean; 16 builds on it.

## Workspace — handed over, do not create a new lane

- Workspace: `/Users/murtaza/manadj-setlist` (jj workspace `setlist`, idle empty change on trunk; lane app :5253 running, sandbox seeded with "test set")
- Registry: `.lanes/setlist.md` — update Agent/Status when you start
- Ports: +80 (backend 8080, vite 5253)
- One jj change per issue: `sets: 06-tempo-policies`, `sets: 14-grace-fade-overlap-load-latency`

## PARALLEL LANE WARNING

Lane `setui` is concurrently implementing issues 09+13 (adjacency-row affordances in `sets/SetDetailPane.tsx`). You own `planner.ts` / `Conductor.ts` / `OverviewLadder.tsx`; they must not touch those. Your 06 UI surfacing (tempo chip, runway warnings) should stay additive in `SetDetailPane.tsx` — new components, appended mounts. Whoever lands second re-rebases + re-gates. Also: lane `setpins` (08+12) is parked and may land mid-session — a rebase over it should be trivial (backend + adjacency.ts).

## Context

- **PRD + issue files are authoritative** (grilled; do not re-litigate): tempo decisions in the PRD (Riding = Tempo return ramps, runway validation, clamp-faster; Fixed = explicit Set tempo defaulted from first track, global rate scaling, tempo-match flag moot; Key Lock inherited per Deck; one policy per Set).
- **What 03–05 built** (their issue Comments are the map): `sets/planner.ts` pure seam (`planSet`, `planStateAt`, discriminated `PlannedAdjacency`, `warnings` computed but UNSURFACED — 06 surfaces them); `sets/Conductor.ts` (rAF driver, drift tolerance 0.12s, freeze-if-loading — 14 splits this into freeze vs late-join); `useSetPlan`, `OverviewLadder.tsx`, seek/playhead/follow (05). No migrations; alembic head `0020_ooytvuot`.
- **Issue 14 note from the orchestrator handoff**: consider promoting the issue's inline truncate-vs-alternatives rationale to an ADR at implementation time (it passes the bar: hard to reverse, surprising, real trade-off).
- **Tempo policy persistence**: per-Set fields (policy + set tempo) — this may be the first sets migration since 0020; check the issues dir/trunk for competing heads before numbering (`<NNNN>_<jj-short-id>` convention), migrate the sandbox clone only.
- **Gate**: `uv run -m pytest`; `npm --prefix frontend run build`; `npx vitest run` (re-run `sets/planner.test.ts` specifically after any rebase — the looping stack, if it lands, touches `vectorize.ts`/`mixModel.ts` which the planner calls); `uv run alembic heads` → one; ruff on touched files.
- `code-review` before parking each issue — it caught real bugs in 04.

## Suggested skills

- `implement`, `tdd` (planner remains the pre-agreed seam)
- `code-review` before parking

## Out of scope

Issues 07, 09, 13 (lane setui), 15 (needs-triage), 16 (queued after you). Never touch other lanes' workspaces.
