# Handoff: setrows batch — sets 26 → 36

Written 2026-07-06 by the orchestrator. You are a fresh implementation session
for the **sets** feature in the manadj repo (jj, NOT git). Two issues,
sequentially, in ONE new lane.

## Task

1. `.scratch/sets/issues/26-unresolved-auto-resolves.md` — model revision:
   Unresolved adjacencies auto-resolve at plan time; explicit Hard-cut pin
   kind. Decisions settled in the body (product-owner decision recorded) —
   do not re-litigate. **Feature → park `ready-for-human`.**
2. `.scratch/sets/issues/36-trailing-suggest-row.md` — trailing ghost suggest
   row replaces the header Suggest button. Decisions in the body.
   **Feature → park `ready-for-human`, stacked on 26's park.**

Both former soft-blocks (rows bundle 31/32/35/33, issue 25) are landed —
you branch off current `main`.

## Read first

1. `docs/agents/parallel-work.md` — lanes, merge-based landing, review gate,
   docs fast-path, hotspot protocol. Binding.
2. `docs/adr/0026-parallel-process-v2-review-gate-merge-landing.md`.
3. `.scratch/sets/PRD.md` + `CONTEXT.md` (glossary: Unresolved, Hard-cut pin,
   Dormant pin, Tempo policy — 26 revises Unresolved semantics; update
   CONTEXT.md in a docs change at the base of your lane if the glossary
   needs it).
4. The two issue files. Acceptance criteria = verification spec.

## Workspace

Fresh lane `setrows`:

- `jj workspace add --name setrows -r main ../manadj-setrows`
  (run from /Users/murtaza/manadj)
- Create `/Users/murtaza/manadj/.lanes/setrows.md` (untracked registry):
  workspace path, your session id as `owner:`, feature `.scratch/sets`,
  claimed areas (SetDetailPane rows/badges/header-suggest, adjacency-view /
  plan-input resolution seam, pin picker, `follow/suggest` UI wiring),
  **port offset +190** (backend 8190, vite 5363), status line.
- Sandbox DB: `cp -c /Users/murtaza/manadj/data/library.db data/library.db`
  from your lane dir. Default workspace is READ-ONLY for you (`.lanes/`
  excepted).

## PARALLEL LANE FENCES

- **conductor lane is LIVE with parked unlanded work** (34 transport routing
  + 38 editor playhead, awaiting human review; may amend in place): it owns
  `Conductor.ts`, conductorStore, keyboard/MIDI transport routing, editor
  playhead overlay, and touches `replan.ts` reads. For 26's resolution
  layer: work at the adjacency-view / plan-input seam **additively**; do NOT
  reshape `Conductor.ts`, conductorStore, or `replan.ts`. If plan-input
  assembly changes shape, leave a tracker comment on 26 describing the seam
  change so the conductor lane can rebase-coordinate.
- Issue 39 (single contextual transport button) is **deferred** — when 36
  removes the header Suggest button, leave the rest of the header transport
  exactly as is.
- Tracker is append-only across lanes; status-line edits only on 26/36.
- Before filing any new issue, check the highest number at `main`
  (currently 40) — five collisions so far.

## Gate

- `uv run -m pytest`; `npm --prefix frontend run build`; `npx vitest run`
  in `frontend/`; `uv run --extra dev ruff check` on touched files.
- 26's resolution logic must be pure + tested at the adjacency/planner seam
  (explicit acceptance criterion). If 26 adds a pin kind to persistence,
  check migration need; HARD invariant: `uv run alembic heads` → one head.
  If you add a migration: `uv run alembic revision --rev-id <NNNN>_<jj-short-id>`.

## Review parks

- Lane app: `uv run scripts/agent/lane_app.py start` (vite
  http://localhost:5363).
- Flip each issue `ready-for-human` with a verification-walkthrough comment
  (URL + 3-5 clicks + what correct looks like); land flips via docs
  fast-path. **Print the full walkthrough in your output message** — the
  human reviews from your transcript.
- After parking 26, continue to 36 on top. Approval lands a prefix.

## Suggested skills

- `implement`; `tdd` (26's favorite-else-most-recent resolution rule is a
  pure seam that wants tests first)

## Out of scope

- Issues 37, 39, 40 (other lanes / deferred)
- Conductor internals, transport routing, editor playhead
- Take-pin semantics (Takes never auto-resolve — spec)

## No secrets

Nothing sensitive here or in referenced files; SoundCloud OAuth token in
backend config is untouched.
