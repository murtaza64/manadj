# Handoff: sets — implement issue 21 (editor claims audibility lazily)

## Task

Implement `.scratch/sets/issues/21-conductor-survives-mode-switch.md` (decision settled, CONTEXT.md Transition editor entry already revised). Gate, `code-review`, **park ready-for-human** with the walkthrough printed in your output — the walkthrough should be the issue's verification matrix, audibly checked.

## Workspace — handed over (idle lane setlist)

- Workspace: `/Users/murtaza/manadj-setlist` — the lane that built the Conductor (04), seek/follow (05), tempo (06), grace fade (14), pickup (16); the audible-surface arbiter and editor lifecycle are all in its recent history
- Registry: `.lanes/setlist.md` — update Agent/Status/claims when you start
- Ports: +80 (backend 8080, vite 5253); re-clone sandbox DB (head 0021)
- One jj change: `sets: 21-conductor-survives-mode-switch`

## Core of the change

Move the Transition editor's `claimAudible('editor')` (plus whatever setup rides it: `mixer.engageAutomation`, pitch snapshots) from mount to the **first audition gesture**; release on unmount only if held. A mounted-but-silent editor stays fully editable. Arbitration at claim time is unchanged — an editor audition still displaces whatever sounds (Conductor stands down per its existing displaced-surface behavior). Verify the matrix in the issue, including: Conductor playing → enter editor → keeps playing → editor play → Conductor stands down → back to Performance → editor pauses (displaced-surface rule); capture stays invisible on all machine paths.

Watch for mount-time assumptions in the editor that silently depend on holding the claim (keyboard transport routing, MIDI gesture registration, automation engage) — registering the surface can stay mount-time; *claiming* is what moves.

## PARALLEL LANE STATE

- Lane `setui` is running the rows bundle (23→18→20) in `SetDetailPane.tsx` — not your files.
- Lane `setpins` is running issue 19 in `sets/planner.ts` — your change should not touch the planner; if you graze `Conductor.ts`, keep it minimal (19 doesn't touch it).

## Gate

`uv run -m pytest`; `npm --prefix frontend run build`; `npx vitest run`; `uv run alembic heads` → one; lint on touched files.

## Suggested skills

- `implement`, `code-review` before parking

## Out of scope

Issues 19, 22, 23/18/20; per-control takeover; any arbiter policy change beyond claim timing.
