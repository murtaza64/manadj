# Handoff: sets — implement issue 19 (hard-cut entry heuristic + plan reactivity)

## Task

Implement `.scratch/sets/issues/19-hard-cut-entry-heuristic.md` (decisions settled; do not re-litigate). Gate, `code-review`, **park ready-for-human** with the walkthrough printed in your output message.

## Workspace — handed over (idle lane setpins)

- Workspace: `/Users/murtaza/manadj-setpins` (jj workspace `setpins`); `jj new main` first if @ isn't already fresh on trunk
- Registry: `.lanes/setpins.md` — update Agent/Status/claims when you start
- Ports: +120 (backend 8120, vite 5293); re-clone sandbox DB (`cp -c /Users/murtaza/manadj/data/library.db data/library.db`) — schema head is now 0021
- One jj change: `sets: 19-hard-cut-entry-heuristic`

## PARALLEL LANE WARNING — planner contention

Lane `setlist` is implementing issue 16 (pickup) and owns `sets/planner.ts` day-to-day. Your change there is SMALL and surgical (hard-cut entry rule + a warning class); keep it minimal, expect a rebase conflict, whoever lands second re-rebases + re-gates. Lane `setui` (07) owns SetDetailPane rows/setStore — your row badge (NEVER AUDIBLE) is one additive chip; if it collides, put the badge logic in your own module and mount minimally.

## Context

- Four parts (see issue): (1) hard-cut entry = incoming's Hot Cue 1 → track start, Main cue out of planning entirely; (2) align 13's practice-affordance unresolved positioning to the same rule; (3) cue/hot-cue persists invalidate the plan's track-facts queries (no reload needed — check `useSetPlan`'s inputs and the persist call sites: `DeckContext`'s cue-set handler, hot-cue mutations); (4) entry-after-exit warning class with real numbers + loud NEVER AUDIBLE row badge (distinct from the issue-14 overlap warning).
- Fresh context: the cue-point PATCH was just fixed (change `pousrpop` — it silently 422'd since November; regression test `tests/test_waveforms_cue.py`). In-app Main-cue persistence works only as of that change; do not assume old cue data reflects user intent.
- Real-world repro for your walkthrough: Set "FUCKBOY SUMMER VOL 1", Slow Motion Ft Mila Falls (track 851) → RUN — Main cue 2:35 sits after the pinned mix-out 1:51 → row showed "plays 0:00". Under the new rule the entry comes from Hot Cue 1 (or start) instead.
- Gate: `uv run -m pytest`; `npm --prefix frontend run build`; `npx vitest run` (planner suite especially); `uv run alembic heads` → one (no migration); lint on touched files.

## Suggested skills

- `implement`, `tdd` (planner seam), `code-review` before parking

## Out of scope

Issues 20 (row polish — soft-blocked on 07), 21 (editor lazy claim), 16/07 (other lanes). No planner reshaping beyond the entry rule + warning class.
