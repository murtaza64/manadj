# Handoff: sets — implement issue 07 (reorder live preview + Dormant pins)

## Task

Implement `.scratch/sets/issues/07-reorder-preview-dormant-pins.md`. Before anything, read the issue's `## Comments`: it carries a 3-point obligations note from the 08/12 session (dormant pins referencing deleted artifacts must be dropped; reconcile with `insertTrackIntoSet` in setStore — issue 10 left the predecessor's pin riding its entry on insert; plus one more) — those deferred criteria are now yours. Gate, `code-review`, then **park ready-for-human** with the walkthrough printed in your output message.

## Workspace — handed over (idle lane setui)

- Workspace: `/Users/murtaza/manadj-setui` (jj workspace `setui`); sandbox DB re-cloned today (schema head incl. 06's tempo fields)
- Registry: `.lanes/setui.md` — update Agent/Status/claims when you start
- Ports: +130 (backend 8130, vite 5303)
- One jj change: `sets: 07-reorder-preview-dormant-pins`

## Design (settled in PRD + glossary — do not re-litigate)

- **Dormant pin** (CONTEXT.md): per-Set, per-ordered-pair memory of a broken pin; restored automatically when that pair becomes adjacent again in that Set (including manually-pinned Takes); strictly per-Set; nothing is ever destroyed by reorder → there is NO discard warning anywhere.
- **Live drag preview**: during a row drag, the overview ladder recomputes optimistically for the hypothetical order, rendering each affected adjacency's future distinctly: will-restore (dormant pin) / auto-fillable (library Transition exists) / unresolved. Drop commits; cancel restores. The planner is a read-only dependency (`useSetPlan` with the hypothetical order) — do NOT modify `sets/planner.ts` semantics; lane `setlist` is concurrently implementing issue 16 against it.
- Persistence: dormancy is Set state — likely your first schema addition since `0020` (check `alembic heads` and the issues dir for competing numbers; `<NNNN>_<jj-short-id>`; migrate the sandbox clone only). Client-authoritative wholesale replace per ADR 0011, like the rest of Set state.

## PARALLEL LANE STATE

- Lane `setlist` is ACTIVE on issue 16 (pickup): owns `sets/planner.ts`, `sets/Conductor.ts`, `conductorStore`, and may touch the Set toolbar. Your work is rows/store/ladder-preview + backend Set persistence. `SetDetailPane.tsx` now contains 17's context menu (SetTrackRow) and 13's practice affordances — all landed; edit additively around them and preserve their behavior (17's menu, hover ▶/✕, practice, drag). Whoever lands second re-rebases + re-gates.
- Issue 18 (row selection + group drag) is ready-for-agent BLOCKED ON YOU — its edge-pin semantics consume your dormancy rules. Keep the restore/degrade logic pure and exported (it will be reused for multi-row blocks); do not implement 18.

## Gate

`uv run -m pytest`; `npm --prefix frontend run build`; `npx vitest run`; `uv run alembic heads` → exactly one; eslint/ruff on touched files.

## Suggested skills

- `implement`, `tdd` (dormancy round-trips are pure-testable; prior art `sets/adjacency.test.ts`), `code-review` before parking

## Out of scope

Issues 16 (setlist lane), 18 (blocked on you); planner/Conductor internals; any discard-warning UI (explicitly rejected — dormancy makes reorder non-destructive).
