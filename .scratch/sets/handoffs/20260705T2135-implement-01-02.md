# Handoff: sets — implement issues 01 + 02

## Task

Implement, in order:

1. `.scratch/sets/issues/01-set-model-sidebar-crud.md`
2. `.scratch/sets/issues/02-adjacency-pins-evidence.md`

Land each at its gate (they are both demoable boundaries). After 02 lands — or earlier if you hit a wall — **stop and write a verification note** (tracker comment on the issue + session summary): what the human should click through to verify. Do not continue to issue 03.

## Workspace — handed over, do not create a new lane

You own the existing quiescent lane `setlist`:

- Workspace: `/Users/murtaza/manadj-setlist` (jj workspace `setlist`, currently an empty change on trunk)
- Registry: `.lanes/setlist.md` in the default workspace — update the Agent/Status lines when you start
- Ports: +80 (backend 8080, vite 5253); always pass explicit ports
- DB: APFS-clone before touching anything with data: `cp -c /Users/murtaza/manadj/data/library.db data/library.db` (from the lane root). Never symlink; migrations run only on the clone until landed (ADR 0012 / docs/agents/parallel-work.md)
- One jj change per issue: `sets: 01-set-model-sidebar-crud`, then `sets: 02-adjacency-pins-evidence`

## Context you need

- **PRD**: `.scratch/sets/PRD.md` — read Implementation Decisions + Testing Decisions fully; they encode a finished grilling. Do not re-litigate.
- **Glossary caveat**: the Set-related CONTEXT.md terms (Set, Dormant pin, Conductor, Tempo policy, Unresolved/Unpracticed…) ride an **unlanded** change in the default workspace and are NOT on trunk yet. The PRD carries the vocabulary you need; do not re-add glossary entries.
- **Persistence pattern**: client-authoritative wholesale replace, per ADR 0011 (see `docs/adr/`) — mirror how Transitions do it (`backend/routers/transitions.py`, `frontend/src/editor/pairStore.ts`).
- **Migrations**: `uv run alembic revision --rev-id <NNNN>_<jj-short-id> -m "<slug>"` — next sequential number; duplicate numbers across lanes are fine, gate enforces one head.
- **Test prior art**: backend `tests/test_takes_router.py`; frontend pure-model suites `frontend/src/follow/model.test.ts`, `frontend/src/editor/transitionIndex.test.ts` (transition index = your evidence source for issue 02).
- **Hotspot protocol** (docs/agents/parallel-work.md): `App.tsx`, `api/client.ts`, `models.py`, `PlaylistSidebar.tsx` are shared — additive-append style only.
- **UI reference**: a throwaway prototype of the eventual Set view lives on the `sets` lane (`/Users/murtaza/manadj-sets/frontend/src/prototype/sets/`, change `qpzwslkl`) with a verdict in its `NOTES.md`. For issues 01–02 you only need: Sets are sidebar siblings of Playlists; the Set detail pane replaces the track table on the browse surface; adjacency rows sit between track rows carrying pin chips + evidence counts + Unresolved/Unpracticed badges. Do NOT copy prototype code — reimplement clean.
- **Evidence definitions** (issue 02): Unresolved = nothing pinned; Unpracticed = ordered pair has zero saved Transitions AND zero Takes. Orthogonal; badge both distinctly.
- **Gate** (before moving `main`): `uv run -m pytest`; `npm --prefix frontend run build`; `npx vitest run` in `frontend/`; `uv run alembic heads` → one head; ruff on touched files. Land per parallel-work.md (short lane: rebase onto trunk tip, gate, `jj bookmark move main --to <head>`, then `jj new`).

## Suggested skills

- `implement` (primary loop)
- `tdd` for the router + any pure model code

## Out of scope for this session

Issues 03–12 (planner, ladder, Conductor, …). The ADR named in issue 02 IS in scope (write it as part of 02).
