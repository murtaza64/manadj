# Handoff: sets — implement issue 25 (pickup/audition dual-driver bug)

## Task

Implement `.scratch/sets/issues/25-pickup-editor-dual-driver.md` (renumbered from 24). The diagnosis is complete and landed (root-cause triple + repro in the issue; a diagnosis harness change also landed — check the issue's Comments for pointers). Both fixes, not one:

1. Pickup predicate refuses non-performance audible-surface holders (unlit + teaching reason, e.g. "editor is auditioning")
2. The Transition editor stands down on displacement (subscribe to holder changes, like the Conductor's own `subscribeAudible` stand-down)

Bugfix policy applies: gate + `code-review`, then this may **auto-land** after agent-owned verification — but print the verification (incl. the repro now failing to wobble) in your output, and prefer parking ready-for-human if anything about the fix changes user-visible semantics beyond the bug itself.

## CRITICAL SEQUENCING CONTEXT

Issue 21 (editor claims audibility LAZILY, on first audition — not on mount) just landed/parked on this same lane. Your fix #2 must be built against 21's final claim lifecycle, not the old mount-time claim. If 21 changed where the editor engages/releases, the displacement subscription belongs in that same lifecycle. Read 21's change and Done comment first. Also verify the interaction matrix from BOTH issues together: Conductor playing → enter editor (keeps playing, per 21) → editor play (Conductor stands down) → Pick up in the Set pane (now UNLIT, per your fix #1 — reason shown) → stop audition → Pick up lights if deck state maps.

## Workspace — handed over (lane setlist)

- Workspace: `/Users/murtaza/manadj-setlist`; `jj new main` onto trunk tip first
- Registry: `.lanes/setlist.md` — update Agent/Status/claims
- Ports: +80 (backend 8080, vite 5253); re-clone sandbox DB if stale
- One jj change: `sets: 25-pickup-editor-dual-driver`

## Gate

`uv run -m pytest`; `npm --prefix frontend run build`; `npx vitest run` (pickup predicate tests live at the planner seam); `uv run alembic heads` → one; lint on touched files. Manual ear-check of the repro is part of verification (the wobble is audible, not assertable).

## Suggested skills

- `implement`, `diagnosing-bugs` (if the landed diagnosis needs re-validation post-21), `code-review`

## Out of scope

Issues 24 (live re-plan — queued after this), 22; per-control takeover; any predicate change beyond the holder check.
