# Handoff: sets — implement issues 10 + 11 (parallel side branch)

## Task

Implement, in order:

1. `.scratch/sets/issues/10-suggestions-append-insert.md` (Set-building suggestions)
2. `.scratch/sets/issues/11-playlist-flows.md` (New Set from Playlist; Create Playlist from Set)

Land each at its gate. After 11 lands — or earlier if you hit a wall — **stop and write a verification note** (tracker comments + session summary) with the human click-through. Do not start other issues.

## Workspace — yours, already created

- Workspace: `/Users/murtaza/manadj-setsugg` (jj workspace `setsugg`, fresh off trunk)
- Registry: `.lanes/setsugg.md` in the default workspace — update Agent/Status when you start
- Ports: +90 (backend 8090, vite 5263); always pass explicit ports
- Sandbox DB clone exists at `data/library.db` (2026-07-05)
- One jj change per issue: `sets: 10-suggestions-append-insert`, `sets: 11-playlist-flows`

## PARALLEL LANE WARNING (read first)

Lane `setlist` (`/Users/murtaza/manadj-setlist`) is concurrently implementing issues 03–04 and WILL touch `frontend/src/sets/SetDetailPane.tsx` and `frontend/src/sets/setStore.ts`. Rules (docs/agents/parallel-work.md):

- Keep your UI in NEW components; touch shared files additive-append only (mount points, appended store methods, appended api resources)
- Rebase onto trunk at issue boundaries only; if trunk moved when you land, rebase + re-gate + retry
- Alembic: 10/11 should need NO migration (both are copies/queries over existing tables). If you think you need one, re-read the issues — you probably don't.

## Context

- **PRD** (settled decisions — do not re-litigate): `.scratch/sets/PRD.md`. Relevant bullets: suggestions reuse Follow-mode tiering, insert ranks by the weaker edge (tie-break stronger), in-Set tracks excluded; playlist flows are one-time copies, no live link; Sets never Export.
- **What 01+02 built** (both `Status: done` — read their `## Comments`): Set CRUD + pins. Client state in `frontend/src/sets/setStore.ts` (ADR 0011 wholesale replace); adjacency/evidence logic in `frontend/src/sets/adjacency.ts`; detail UI in `frontend/src/sets/SetDetailPane.tsx`; evidence sources are `editor/pairStore.ts` (transitions) and the `['takes']` query.
- **Issue 10**: pure ranking functions live beside `frontend/src/follow/model.ts` (tier logic + `getHarmonicKeys` already there; known-tier inputs via `links/known.ts` and `editor/transitionIndex.ts`). Test in the style of `follow/model.test.ts` — that suite is the seam. Direction matters: append ranks candidates OUT OF the last track; insert scores out-of-predecessor AND into-successor, combined by the weaker edge. Accepting a suggestion appends/inserts the Track with the usual auto-fill offer (02 built auto-fill).
- **Issue 11**: playlists API already has CRUD (`backend/routers/playlists.py`, `api.playlists.*`). "New Set from Playlist" copies Play order into a new Set + offers auto-fill for eligible adjacencies; "Create Playlist from Set" copies track order into a new Playlist (which then Exports like any Playlist). No live link either direction.
- **Gate**: `uv run -m pytest`; `npm --prefix frontend run build`; `npx vitest run` in `frontend/`; `uv run alembic heads` → one head; ruff on touched files. Land per parallel-work.md; docs fast-path for tracker comments.
- **Glossary caveat**: Set-related CONTEXT.md terms may ride an unlanded change in the default workspace; the PRD carries the vocabulary. Do not add glossary entries.

## Suggested skills

- `implement` (primary loop)
- `tdd` (ranking functions — pure seam)

## Out of scope

Issues 03–09, 12. Anything touching the planner, ladder, or Conductor (the other lane owns those).
