# Handoff: conductor batch — sets 28 → 34 → 38

Written 2026-07-06 by the orchestrator. You are a fresh implementation session
for the **sets** feature in the manadj repo (jj, NOT git). You will work three
issues sequentially in ONE new lane.

## Task

Implement, in order:

1. `.scratch/sets/issues/28-foreign-loads-during-conduction.md` — **bugfix**.
   Decision is settled in its Comments (foreign load = takeover). Landing
   policy: auto-land after your own verification (per docs/agents/parallel-work.md).
2. `.scratch/sets/issues/34-transport-input-routing.md` — **feature**.
   Decisions settled in the body — do not re-litigate. Park `ready-for-human`.
3. `.scratch/sets/issues/38-editor-live-conductor-playhead.md` — **feature**.
   Spec is in the body (builds on sets 24's `replan.ts`, landed on main).
   Park `ready-for-human`, stacked on 34's park; keep the lane app running.

Parks stack: after parking 34, continue to 38 on top per the review-gate flow
(docs/agents/parallel-work.md "Requesting review"). Human approval lands a
prefix.

## Read first

1. `docs/agents/parallel-work.md` — lanes, landing (merge-based), review gate,
   docs fast-path, hotspot protocol. Binding.
2. `docs/adr/0026-parallel-process-v2-review-gate-merge-landing.md` — the process ADR.
3. `.scratch/sets/PRD.md` + `CONTEXT.md` — feature design + glossary
   (Conductor, Pickup, takeover, Hard-cut pin…). Use the glossary vocabulary.
4. The three issue files above — the issue text is the spec; acceptance
   criteria are the verification spec.

## Workspace

Open a **fresh lane** named `conductor`:

- `jj workspace add --name conductor -r main ../manadj-conductor`
  (run from /Users/murtaza/manadj)
- Create `.lanes/conductor.md` (in /Users/murtaza/manadj/.lanes/ — untracked,
  writable from anywhere) per the registry format: workspace path, your
  session id as `owner:`, feature `.scratch/sets`, claimed areas
  (`frontend/src/sets/Conductor.ts` + conductor stores, keyboard/MIDI
  transport routing, editor playhead overlay), **port offset +100**
  (backend 8100, vite 5273), status line.
- Sandbox DB: `cp -c /Users/murtaza/manadj/data/library.db data/library.db`
  (APFS clone, from your lane dir). Never touch the default workspace's data/.
- The default workspace (/Users/murtaza/manadj) is READ-ONLY for you;
  `.lanes/` is the sole exception.

## PARALLEL LANE FENCES

- **setui lane is LIVE** (rows batch 32 → 35 → 33): owns `SetDetailPane.tsx`
  set-row rendering and, in its final issue (33), controller browse/load MIDI
  mapping. Do NOT reshape set rows. For issue 34's MIDI dispatch work:
  additive-append style only, touch only transport routing; check
  `.lanes/setui.md` before starting 34 and leave a tracker comment on 34
  noting exactly what you changed in the MIDI dispatch so setui's 33 can
  coordinate.
- **orchestrator lane**: docs only; owns nothing you need.
- Tracker (`.scratch/`) is append-only across lanes: comments under
  `## Comments`; status-line edits only on your three issues.
- Issue filing: if you must file a new issue, check the highest number at
  `main` first (currently 39) — we have had five numbering collisions.

## Gate (verification, you own it)

Acceptance criteria in each issue = the spec. Standard suite as a suggestion:

- `uv run -m pytest`
- `npm --prefix frontend run build` and `npx vitest run` in `frontend/`
- `uv run --extra dev ruff check` on touched files (known baseline excepted)
- HARD invariant, always: `uv run alembic heads` → exactly one head
  (no migrations expected in this batch)

Landing is merge-based: `jj new <lane-head> <main-commit> -m "land: …"`,
verify that tree, `jj bookmark move main --to <merge>`. `main` only moves to
descendants of its current position. After auto-landing 28, do the
post-landing hot-reload check per parallel-work.md.

## Review parks (34, 38)

- Start the lane app: `uv run scripts/agent/lane_app.py start`
  (vite http://localhost:5273).
- Flip the issue to `Status: ready-for-human` with a verification-walkthrough
  comment (URL + the 3-5 clicks and what correct looks like); land the flip
  via the docs fast-path.
- **Print the full walkthrough in your output message itself** — URL and
  click-by-click, not a pointer. The human reviews from your transcript.

## Suggested skills

- `implement` — the working loop per issue
- `tdd` — 28's takeover rule and 34's space-priority resolution are
  pure-logic seams that want tests first; 38's authored-axis mapping too
- `diagnosing-bugs` — if 28's reload war resists the settled fix

## Out of scope

- Issues 26, 36, 39 (SetDetailPane header/rows — held behind setui's batch)
- Any Conductor semantics beyond the three issue specs; no re-litigating
  settled decisions
- Migrations, backend model changes (none expected)

## No secrets

Nothing sensitive here or in the referenced files. The SoundCloud OAuth token
in backend config is untouched by this work.
