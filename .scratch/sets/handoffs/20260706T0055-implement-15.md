# Handoff: sets — implement issue 15 (automation ghosts)

## Task

Implement `.scratch/sets/issues/15-visualize-conductor-automation.md`. The **Agent Brief in the issue is the contract** — read it fully; the Decisions section (screen-only v1, ghost-as-second-indicator, DAW-modeled) is settled, do not re-litigate.

Gate, then **park ready-for-human** with a review-request comment; per the new convention (docs/agents/parallel-work.md), print the review walkthrough in your output message, not only in docs/toast. Expect a visual-iteration round with the human — that's by design; park early rather than gold-plate the styling.

## Workspace — handed over (idle lane setpins)

- Workspace: `/Users/murtaza/manadj-setpins` (jj workspace `setpins`, idle empty change; rebase/`jj new main` onto current trunk before starting — it parked on an older tip)
- Registry: `.lanes/setpins.md` — update Agent/Status/claimed-areas when you start (claim: mixer read accessor + MixerStrip ghost rendering)
- Ports: +120 (backend 8120, vite 5293); sandbox DB clone exists (seeded Set "Lifecycle demo" — fine for testing: pin a transition, play via Conductor, watch ghosts)
- One jj change: `sets: 15-visualize-conductor-automation`

## PARALLEL LANE WARNING

- Lane `setlist` (06+14) owns `sets/planner.ts`, `sets/Conductor.ts`, `OverviewLadder.tsx`. Your Mixer change must be a READ-ONLY accessor — zero changes to the automation write path, engage/disengage, or base-state semantics (the brief's out-of-scope; also keeps you conflict-free with their Conductor work).
- Lane `setui` (09+13) owns adjacency-row affordances in `sets/SetDetailPane.tsx` — you shouldn't need that file at all.
- Whoever lands second re-rebases + re-gates.

## Context

- The Mixer's automation overlay (ADR 0022) owns the 10 lane-driven params; base state is never mutated by automation and subscribers are never notified — your accessor is polled per-frame by the view, mirroring how waveform playheads are rAF-driven rather than store-driven.
- Both engagers benefit: the Conductor and the Transition editor's mix player go through the same overlay — verify ghosts in both.
- Takeover semantics must be byte-identical after your change (a drag mid-automation writes base state and stops the Conductor exactly as today).
- **Gate**: `uv run -m pytest`; `npm --prefix frontend run build`; `npx vitest run`; `uv run alembic heads` → one (no migration expected); ruff/eslint on touched files. `code-review` before parking.

## Suggested skills

- `implement`; `code-review` before parking

## Out of scope

Everything in the brief's out-of-scope, all other sets issues, other lanes' files.
