# Handoff: sets — implement issues 08 + 12 (backend-leaning parallel branch)

## Task

Implement, in order:

1. `.scratch/sets/issues/08-promotion-repoints-pins.md` (Take promotion re-points Set pins)
2. `.scratch/sets/issues/12-lifecycle-edges.md` (dangling pins degrade; Archived-track flag)

Gate each; **park the stack ready-for-human** with review-request comments (feature work never advances `main` without the human's verbal approval — docs/agents/parallel-work.md). After 12 is parked — or earlier if you hit a wall — stop and write a verification note.

## Workspace — yours, already created

- Workspace: `/Users/murtaza/manadj-setpins` (jj workspace `setpins`, fresh off trunk)
- Registry: `.lanes/setpins.md` — update Agent/Status when you start
- Ports: +120 (backend 8120, vite 5293) — check `.lanes/` for collisions before binding (a +90 collision happened recently)
- Sandbox DB clone at `data/library.db` (2026-07-05); migrations run on the clone only
- One jj change per issue: `sets: 08-promotion-repoints-pins`, `sets: 12-lifecycle-edges`

## PARALLEL LANE WARNING (read first)

Lane `setlist` is actively iterating a PARKED stack for issues 03+04 (planner, `sets/planner.ts`; ladder, `sets/OverviewLadder.tsx`; Conductor, `sets/Conductor.ts`; heavy edits to `sets/SetDetailPane.tsx`). Rules:

- Do NOT touch planner/ladder/Conductor files at all.
- `SetDetailPane.tsx` / sidebar: at most one additive badge mount for 12's archived flag — keep the flag logic in your own module. If even that feels contentious, surface the flag in the sidebar Sets section only and leave the detail pane alone.
- Their stack is unlanded; your conflicts materialize at landing time — whoever lands second re-rebases + re-gates.

## Context

- **PRD** (settled; do not re-litigate): `.scratch/sets/PRD.md`.
- **Issues 01+02 structures** (both done — read their `## Comments`): pins live on the Set entry heading the adjacency (`set_entries.pin_kind`/`pin_uuid`, deliberately no FK); dangling refs already degrade client-side in `frontend/src/sets/adjacency.ts`. Alembic head `0020_ooytvuot`.
- **Issue 08**: the promotion hook is described in `docs/adr/0023-set-adjacencies-may-pin-unpromoted-takes.md` and the PRD — at the take-promotion endpoint, rewrite every Set pin referencing that Take uuid to the new Transition uuid (one-time migration, not query-time indirection). Router-test it in the style of `tests/test_takes_router.py`.
- **Issue 12 scope adjustment**: the "Dormant pins referencing deleted artifacts are dropped" criterion is NOT yet implementable — Dormant pins are issue 07 (unstarted, owned by the setlist lane's future work). Implement the other criteria (active-pin degradation server- and client-consistent; Archived-track flag on Sets; planner-independent — do not touch the planner, its hard-cut degradation already exists). Leave a tracker comment on 12 noting the dormant criterion is deferred to 07, and a comment on 07 telling it to (a) drop dormant pins referencing deleted artifacts, (b) reconcile with `insertTrackIntoSet` in `setStore.ts` (see issue 10's comments — insert leaves the predecessor's pin on its entry).
- **Gate**: `uv run -m pytest`; `npm --prefix frontend run build`; `npx vitest run` in `frontend/`; `uv run alembic heads` → one head; ruff on touched files. Docs fast-path for tracker comments.
- **Glossary caveat**: Set-related CONTEXT.md terms may still ride an unlanded default-workspace change; the PRD carries the vocabulary.

## Suggested skills

- `implement`, `tdd` (router seams)

## Out of scope

Issues 03–07, 09 (03/04 parked on lane setlist; 05/06/07/09 blocked on them). Anything in planner/ladder/Conductor territory.
