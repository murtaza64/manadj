# Handoff: sets — implement issue 24 (live re-plan)

## Task

Implement `.scratch/sets/issues/24-live-replan.md` (design settled in the issue + PRD Comments; the sounding-window rule and the reorder-removes-anchor fallback are decided — the latter with explicit permission to propose better in the park comment if implementation reveals a cleaner behavior). Gate, `code-review`, **park ready-for-human** with the walkthrough printed in your output — the walkthrough should include the ear-checkable loop: set playing → edit a downstream transition in the editor → hear it at the handover; edit the sounding window's lanes → hear it immediately.

## Process — ADR 0026 (parallel process v2)

Feature work parks ready-for-human; landing is merge-based (see docs/agents/parallel-work.md); default workspace read-only; set `owner: <your session id>` in `.lanes/setlist.md`.

## Workspace — handed over (idle lane setlist)

- Workspace: `/Users/murtaza/manadj-setlist`; `jj new main` onto trunk tip first; re-clone sandbox DB if stale
- Ports: +80 (backend 8080, vite 5253)
- One jj change: `sets: 24-live-replan`

## Context — you inherit the full Conductor stack (this lane built it)

- 04 (Conductor, plan snapshot — the thing you're replacing), 05 (`planStateAt` seek), 06 (tempo ramps), 14 (grace fade / plan-tail rewrites, ADR 0025 — re-plan must compose with tail rewrites, read that ADR), 16 (Pickup mapping — your remap engine), 21 (lazy editor claim — what makes "edit while conducting" possible), 25 (JUST LANDED: pickup predicate refuses non-performance holders + editor displacement stand-down).
- **25 interaction, important**: the user-facing Pickup predicate now checks the audible-surface holder. Live re-plan's internal remap runs while the CONDUCTOR itself is the holder — that's the normal case, not a refusal case. Reuse the pure mapping functions, not the user-facing predicate gate; do not let 25's holder check block internal remaps.
- Plan-input subscription: transitions (pair store autosaves), pins/dormancy (setStore), tempo policy, order. Note issue 26 (unresolved auto-resolves) is QUEUED and will widen plan inputs later — keep the subscription seam clean so 26 plugs in without rework.
- Debounce/coalesce edits sanely (editor drags emit streams); the sounding-window geometry deferral is per the issue.

## PARALLEL LANE STATE

- `setui` (22, rows/toolbar styling) and `setsugg` (30, ladder waveform rendering) — not your files. `setpins` on a planner bpm-authority bugfix — planner contention possible: coordinate via `.lanes/`, small surgical diffs, lands-second-merges.

## Gate

`uv run -m pytest`; `npm --prefix frontend run build`; `npx vitest run` (planner suite especially); `uv run alembic heads` → one; lint on touched files.

## Suggested skills

- `implement`, `tdd` (remap logic pure at the planner seam), `code-review` before parking

## Out of scope

Issue 26 (queued next), 22/30 (other lanes), any change to Pickup's user-facing predicate or the takeover rules.
