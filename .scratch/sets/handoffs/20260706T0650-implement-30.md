# Handoff: sets — implement issue 30 (ladder waveforms follow global styles)

## Task

Implement `.scratch/sets/issues/30-ladder-waveforms-follow-styles.md` (requirement + constraints are settled; mechanism is your choice within them — document it in the park comment). Gate, `code-review`, then **park ready-for-human** with the walkthrough printed in your output (visual work: expect an iteration round; a before/after against the styles mode is the ideal walkthrough).

## Process — parallel process v2 (ADR 0026, NEW — read it + docs/agents/parallel-work.md first)

- Feature work parks ready-for-human; only human approval moves `main`
- **Landing is merge-based now** (landing merge pinning the verified `main`; retries = new merges; never rewrite the stack) — follow the process doc, not older handoffs' rebase-landing instructions
- The default workspace is read-only; all work (docs included) in your lane; update `.lanes/setsugg.md` with `owner: <your session id>` when you start

## Workspace — handed over (idle lane setsugg)

- Workspace: `/Users/murtaza/manadj-setsugg`; `jj new main` onto trunk tip before starting
- Ports: +110 (backend 8110, vite 5283); re-clone sandbox DB if you want fresh data (styling work — optional)
- One jj change: `sets: 30-ladder-waveforms-follow-styles`

## Context

- `sets/OverviewLadder.tsx` clips render via `toThreeBands` + private canvas colors (prototype idiom) — bypassing the Waveform style system entirely. The style system: named render recipes (shader variant + band grouping / per-group gain / gamma / smoothing), persisted style slots, edited live in styles mode (`waveform/StyleTuningPage`); player surfaces honor it via the V2 renderer (`useWaveformRendererV2`).
- Hard constraints from the issue: NO per-clip WebGL contexts (40+ clips; browser context caps) — CPU/2D interpretation of the style parameters or a shared-context/atlas approach; live re-render on style change; ladder scroll perf unregressed; mirrored-lane layout wins over style orientation; fidelity target "recognizably the same style" at ~40px, simplifications documented.
- Study how the V2 renderer consumes style slots to reuse the *parameters* (and color definitions) even if you don't reuse the shader.

## PARALLEL LANE STATE

- Lane `setui` is restyling Set-view ROWS + toolbar (issue 22) — `OverviewLadder.tsx` internals are yours alone, but you share `SetDetailPane.tsx` only at the ladder's mount point: don't touch anything else there. New rows tickets are expected from that session — irrelevant to ladder internals.
- Lanes `setlist` (25) and `setpins` (planner bpm-authority bugfix) — not your files.

## Gate

`uv run -m pytest`; `npm --prefix frontend run build`; `npx vitest run`; `uv run alembic heads` → one (no migration); lint on touched files.

## Suggested skills

- `implement`, `code-review` before parking

## Out of scope

Row/toolbar styling (22), ladder interaction behavior (scroll pinning, seek, dimming), style-system changes (consume, don't extend — comment on the styles tracker if you find a gap).
