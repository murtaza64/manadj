# Handoff: topbar — sets 40 audio-ownership chip

Written 2026-07-06 by the orchestrator. You are a fresh implementation session
for the **sets** feature in the manadj repo (jj, NOT git). One issue, one new
lane.

## Task

`.scratch/sets/issues/40-audio-ownership-chip.md` — persistent TopBar chip
showing who owns the decks/audio (SET playing/paused, AUDITION, muted DECKS).
Grilled 2026-07-06; decisions in the body are settled — do not re-litigate.
**Feature → park `ready-for-human`.**

## Read first

1. `docs/agents/parallel-work.md` — lanes, merge-based landing, review gate,
   docs fast-path, hotspot protocol. Binding.
2. `docs/adr/0026-parallel-process-v2-review-gate-merge-landing.md`.
3. `docs/adr/0024-*` (Audible surfaces / conductor claim) — the chip
   visualizes exactly this model.
4. `.scratch/sets/PRD.md` + `CONTEXT.md` glossary.
5. The issue file. Acceptance criteria = verification spec. Everything you
   need is landed: `audibleHolder`/`subscribeAudible`
   (`playback/audibleSurface.ts`), `useConductorState`, `['sets']` query.

## Workspace

Fresh lane `topbar`:

- `jj workspace add --name topbar -r main ../manadj-topbar`
  (run from /Users/murtaza/manadj)
- Create `/Users/murtaza/manadj/.lanes/topbar.md` (untracked registry):
  workspace path, your session id as `owner:`, feature `.scratch/sets`,
  claimed areas (TopBar chrome chip; App.tsx additive mount only),
  **port offset +200** (backend 8200, vite 5373), status line.
- Sandbox DB: `cp -c /Users/murtaza/manadj/data/library.db data/library.db`
  from your lane dir. Default workspace is READ-ONLY for you (`.lanes/`
  excepted).

## PARALLEL LANE FENCES

- **App.tsx / TopBar is a structural hotspot** (parallel-work.md): additive-
  append mounting only; no reshaping of chrome layout beyond the chip's slot.
- **conductor lane** has 34 (transport input routing) parked unlanded. The
  chip's tooltip copy describes next-gesture consequences per 34's DECIDED
  semantics (space drives the Conductor when a set is selected). Write the
  tooltip to 34's spec; if 34 has not landed when you park, say so in your
  walkthrough so the human reviews the copy knowingly. Do not touch keyboard/
  MIDI routing yourself.
- **setrows lane** (26+36, spawned alongside you) owns SetDetailPane
  rows/header — stay out; your click-navigation to the conducting set goes
  through existing selection/navigation entry points.
- Tracker is append-only across lanes; status-line edits only on 40.
- Before filing any new issue, check the highest number at `main`
  (currently 40).

## Gate

- `uv run -m pytest`; `npm --prefix frontend run build`; `npx vitest run`
  in `frontend/`; `uv run --extra dev ruff check` on touched files.
- HARD invariant: `uv run alembic heads` → one head (no migration expected —
  frontend-only).
- Bright, fully saturated colors (repo convention — no pastels).

## Review park

- Lane app: `uv run scripts/agent/lane_app.py start` (vite
  http://localhost:5373).
- Flip 40 `ready-for-human` with a verification-walkthrough comment (URL +
  the clicks: start a set → chip shows ▶ SET; pause → ⏸ SET persists; deck
  gesture takeover → muted DECKS; editor audition → AUDITION; click-navigate
  checks). Land the flip via docs fast-path. **Print the full walkthrough in
  your output message** — the human reviews from your transcript.

## Suggested skills

- `implement`; `tdd` for the face-selection logic (audibleHolder × conductor
  status → face) as a pure seam

## Out of scope

- Any transport control on the chip (navigate-only — settled)
- The system-initiated stand-down toast (explicitly a follow-up candidate)
- Issues 26/36/37 (other lanes)

## No secrets

Nothing sensitive here or in referenced files; SoundCloud OAuth token in
backend config is untouched.
