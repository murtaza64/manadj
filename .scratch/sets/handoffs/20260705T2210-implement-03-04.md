# Handoff: sets — implement issues 03 + 04

## Task

Implement, in order:

1. `.scratch/sets/issues/03-planner-overview-ladder.md` (pure planner + overview ladder)
2. `.scratch/sets/issues/04-conductor-v1.md` (Conductor: play a Set end-to-end)

Land each at its gate (both are demoable boundaries). After 04 lands — or earlier if you hit a wall — **stop and write a verification note** (tracker comments + session summary) with the click-through: ladder visual checks and an audible end-to-end Set playback script. Do not start issues 05+.

## Workspace — handed over, do not create a new lane

You own the existing quiescent lane `setlist`:

- Workspace: `/Users/murtaza/manadj-setlist` (jj workspace `setlist`, empty change on trunk tip)
- Registry: `.lanes/setlist.md` in the default workspace — update Agent/Status when you start
- Ports: +80 (backend 8080, vite 5253); the lane app may still be running from the last session — reuse or restart via `uv run scripts/agent/lane_app.py`
- Sandbox DB clone exists at `data/library.db` (2026-07-05); re-clone from the default workspace for fresher data if needed
- One jj change per issue: `sets: 03-planner-overview-ladder`, `sets: 04-conductor-v1`

## What the previous session left you (do not re-do)

Issues 01+02 are `Status: done`, landed, human-verified — read their `## Comments` for the click-through baseline. Key structures you build on:

- Pins live on the Set entry that HEADS the adjacency (`pin_kind`/`pin_uuid`, no FK); dangling refs degrade client-side in `frontend/src/sets/adjacency.ts`
- Client state: `frontend/src/sets/setStore.ts` (ADR 0011 wholesale replace); evidence sources: `editor/pairStore.ts` (transitions) + the `['takes']` query, wired in `SetDetailPane.tsx`
- Alembic head `0020_ooytvuot`; ADR 0023 (Take pinning) exists

## Context for 03 (planner + ladder)

- The planner is the PRD's designated pure seam — TDD it (prior art: `frontend/src/editor/mixModel.test.ts`, `capture/vectorize.test.ts`). All Set-playback semantics live there: ping-pong parity, entry/exit, mix offsets, hard cuts (outgoing to end → incoming Main cue; first track starts at Main cue), Take pins planned via the existing vectorizer (`capture/vectorize.ts`) at plan time.
- Ladder UI: follow the prototype verdict at `/Users/murtaza/manadj-sets/frontend/src/prototype/sets/NOTES.md` (change `qpzwslkl` on the `sets` lane; the .tsx files there are reference for geometry/interaction). Reimplement clean — never copy prototype code. Essentials: zoomed staircase, mirrored lanes around a center line, titles outside waveforms on the outer edge, hot cues (real ones — the prototype faked them) line+triangle on the title side, hard cuts as dashed red blade + ✕, ladder/list scroll pinned via one progress value (list scroll fraction — pure centering fails at edges).
- Note: ladder click-to-seek is issue 05, NOT now. For 03, ladder click may scroll the list (prototype behavior) or do nothing — your call, it gets replaced.

## Context for 04 (Conductor)

- Runtime driver over the plan, in the mold of `frontend/src/editor/MixPlayer.ts`, playing through the shared DeckEngines + Mixer (ADR 0022 pattern — the editor already conducts shared machinery; study how it claims the audible surface, ADR 0013).
- Roles, not decks: Transition lanes (`faderA`…) mean outgoing/incoming; swap channel mapping on odd adjacencies.
- Takeover: any manual deck/mixer gesture stops the Conductor entirely; decks keep playing as-is. Conductor transport (play/pause, row play buttons) is NOT a takeover trigger.
- Capture invisibility: Conductor-driven playback must not generate Takes (see how editor auditions stay invisible to the capture layer); capture resumes at takeover.
- Row play button = start at that track's planned entry. Full seek/playhead/follow is issue 05 — out of scope.
- Tempo v1: tempo-match during windows, snap to native after. Riding ramps / Fixed policy are issue 06 — out of scope.
- Write the ADR "Set playback is a Conductor, not an Audible surface" as part of 04 (rationale: takeover + capture-invisibility rules; a reader of ADR 0013 expects a third surface).

## Glossary caveat

Set-related CONTEXT.md terms may still ride an unlanded change in the default workspace — check trunk before assuming; the PRD carries all vocabulary. Do not re-add glossary entries.

## Suggested skills

- `implement` (primary loop)
- `tdd` (planner seam)

## Out of scope

Issues 05–12 (seek/follow, tempo policies, dormant pins, promotion re-point, editor click-through, suggestions, playlist flows, lifecycle edges).
