# Handoff: sets — implement issues 23 → 18 → 20 (the rows bundle)

## Task

Implement, in this order (each builds on the previous):

1. `.scratch/sets/issues/23-list-drag-preview.md` — extend 07's drag preview from the ladder to the list (renumbered from 20 today; read its Implementation notes — they came from the 07 session that built the machinery you'll consume)
2. `.scratch/sets/issues/18-set-row-selection-group-drag.md` — selection model + group drag (upgrades 23's preview to blocks; Decisions + Agent Brief in the issue are settled)
3. `.scratch/sets/issues/20-set-row-polish.md` — the styling batch LAST, so it styles the final row DOM (edit button, deck-gradient bars, left [+], alignment, plain practice button, red hard-cut chip)

Gate + `code-review` per issue; **park each ready-for-human** with walkthroughs printed in your output. One jj change per issue (`sets: NN-slug`). Stop after 20 parks.

## Workspace — handed over (idle lane setui)

- Workspace: `/Users/murtaza/manadj-setui` — the SAME lane that implemented 07, so `sets/dormancy.ts` (`reconcileOrderChange`, `previewAdjacencyFutures`), the drag refs, and the ladder preview are your own recent work-product; read 07's Done comments to re-orient
- Registry: `.lanes/setui.md` — update Agent/Status/claims when you start
- Ports: +130 (backend 8130, vite 5303); re-clone sandbox DB (schema head 0021)
- Trunk includes 16 (pickup) and 17 (context menu) — the rows now carry 17's context menu and 13's practice affordances; preserve their behavior (17's targeting rule is selection-aware by design — 18 must make right-click-with-selection act on the selection, verify not rebuild)

## PARALLEL LANE STATE

- Lane `setpins` is running issue 19 (planner entry rule + a NEVER AUDIBLE row badge — one additive chip in your rows). Whoever lands second re-rebases; the collision is one chip mount, keep it trivial.
- Lane `setlist` is being dispatched issue 21 (editor lazy audibility claim) — different files entirely.
- Nobody else touches `SetDetailPane.tsx`/`setStore.ts`/`dormancy.ts` — you own the rows for this session.

## Gate

`uv run -m pytest`; `npm --prefix frontend run build`; `npx vitest run`; `uv run alembic heads` → one (no migrations); lint on touched files.

## Suggested skills

- `implement`, `tdd` (selection/compaction logic is pure-testable; dormancy round-trip prior art in your own 07 tests), `code-review` before parking

## Out of scope

Issues 19, 21, 22 (needs-triage); planner/Conductor internals; ladder internals beyond what 23's preview consumption requires.
