# 32 — Adjacency rows show the transition (overlap) time

Status: ready-for-human

## Parent

.scratch/sets/PRD.md (refinement batch 2026-07-06, same pass as issue 31)

## What to build

The adjacency (transition) row shows how long the handover overlaps: the
planned window span on the mix axis — `PlannedAdjacency.mixEndSec −
mixStartSec` (`frontend/src/sets/planner.ts`; hard cuts have
`mixStart === mixEnd`, i.e. 0:00 — render nothing or an explicit 0:00,
decide at triage alongside the red hard-cut chip from sets 20).

Placement follows issue 31's column grid: the overlap time sits in the
same time-column band as the track rows' in/play times, so the whole
list reads as one table.

## Acceptance criteria

- [ ] Pinned adjacencies show the planned overlap duration (`fmtSec`),
      aligned to the time columns of the neighboring track rows
- [ ] Hard cuts render per the triaged decision (blank vs 0:00), never a
      misleading nonzero span
- [ ] Live drag preview (sets 23) shows the hypothetical order's overlap
      times (same `displayPlan` source as the rest of the row)
- [ ] No behavioral changes — display only

## Blocked by

- 31-set-row-columns (soft: shares the column grid; can land together)

(Renumbered from 31, 2026-07-06 — cascade of the 30 collision.)

## Comments

> *This was generated by AI during triage.*

Triage 2026-07-06: hard cuts render BLANK in the overlap column (the red hard-cut chip carries the message; 0:00 is noise).

Implemented 2026-07-06 (setui lane, jj change `qskormqx` — `sets: 32-adjacency-overlap-time`, on top of 31's grid). Notes:

- `fmtOverlapTime` in `rowColumns.ts` (pure, tested): windowed adjacency →
  `fmtSec(mixEndSec − mixStartSec)`; hard cut → blank per triage; blank
  while the plan loads.
- The cell sits exactly under the track rows' play-time column: a new
  `ADJ_TIME_SPACER_W` (derived from the grid constants, alignment locked
  by a test) stands in for the ✕ column.
- Preview: the cell reads `displayPlan.adjacencies[i]` — the same source
  the rest of the row uses, so drag previews show the hypothetical spans.

## Verification walkthrough (ready-for-human)

Lane app on setui: http://localhost:5313 (or
`npm --prefix desktop start -- --port 5313`). Review tree includes 31.

1. Open the demo Set. Every PINNED adjacency row now shows an overlap
   duration (m:ss) at the right, exactly under the track rows'
   "play" column — the list reads as one table.
2. Unpinned (red "✕ hard cut") adjacencies show NOTHING in that column —
   no 0:00.
3. Pin/unpin an adjacency (click the pin chip): the overlap time appears/
   disappears with the pin.
4. Drag a row: mid-drag the overlap times update to the hypothetical
   order (same displayPlan as the rest of the preview).
