# 31 — Columnar Set track rows (BPM/key left, color-coded; time columns)

Status: ready-for-human

## Parent

.scratch/sets/PRD.md (refinement batch 2026-07-06, from live use after the
20-set-row-polish landing)

## What to build

Restructure the Set track row into an explicit column grid — columnar
design is the direction for these rows going forward (shared column
constants, not per-row flex guesswork; today's 81px chip alignment in
the adjacency rows is derived by hand from the track row's flex metrics
and silently breaks if either side changes).

1. **BPM and key move LEFT, right after the play-order index** — fixed-
   width columns so values align down the list (they sit right-side
   today, `SetDetailPane.tsx` SetTrackRow: key 40px, BPM 64px after the
   plays-span).
2. **Color-code BPM and key.** Open decisions for triage:
   - Key: color by key identity (e.g. Camelot-wheel hue) vs. by
     compatibility with the neighboring tracks?
   - BPM: absolute value bands vs. delta against the Set tempo /
     neighbors? (Fixed policy makes "delta vs Set tempo" natural;
     Riding has no single reference.)
   - Bright, saturated colors per repo convention; cyan/magenta stay
     Deck identity (CONTEXT.md "Deck color") — pick outside that pair.
3. **Time columns replace the single "plays m:ss of m:ss" span**:
   - **in time** — the mix-clock time this track enters
     (`PlannedEntry.entryMixSec`);
   - **play time** — the audible span, rendered as `play/total`
     (today's `exitSec − entrySec` over `duration_secs`).
   - NEVER AUDIBLE (sets 19) keeps its badge behavior in this area.
4. **Shared column geometry**: extract the column widths/offsets to
   module-level constants consumed by both SetTrackRow and AdjacencyRow
   (replaces AdjacencyRow's hand-derived `GUTTER_WIDTH = 58` math from
   sets 20; issue 32's overlap-time column aligns to the same grid).

## Acceptance criteria

- [ ] Row reads: ▶ · # · key · BPM · title/artist · … · time columns · ✕
      — key/BPM and the time columns align as columns across all rows
- [ ] Key and BPM render color-coded per the triaged scheme
- [ ] "in time" shows the plan's entry mix time; "play time" shows
      play/total; NEVER AUDIBLE unchanged
- [ ] Adjacency-row chip alignment (sets 20) survives, now driven by the
      shared column constants
- [ ] No behavioral changes — layout, color, and formatting only

## Notes

- `frontend/src/sets/SetDetailPane.tsx` rows; plan fields already carry
  everything needed (`useSetPlan` → `PlannedEntry`).
- Selection ring / conducting wash / drag dimming (sets 18/23) must
  still read clearly over the new columns.

## Blocked by

- (none — lands on top of 20's row DOM)

(Renumbered from 30, 2026-07-06 — parallel filing collided with 30-ladder-waveforms-follow-styles.)

## Comments

> *This was generated by AI during triage.*

Triage 2026-07-06: key color = KEY IDENTITY (Camelot-wheel hue; compatibility coloring shifts with reorders and is ambiguous about which neighbor — compatibility signals stay in suggestions). BPM color = DELTA semantics: vs Set tempo under Fixed, vs predecessor BPM under Riding; rendered text remains the absolute BPM. Deck cyan/magenta reserved per glossary.

Implemented 2026-07-06 (setui lane, jj change `zqqlwplx` — `sets: 31-set-row-columns`). Notes:

- New `frontend/src/sets/rowColumns.ts`: shared column constants (both row
  components consume them; row gap/padding now ride inline from the same
  constants, so the CSS has no geometry to drift), plus the pure BPM-delta
  and time-column rules under vitest (`rowColumns.test.ts`).
- Key color reuses the app's existing Camelot-hue identity mapping
  (`getKeyColor`, the library's KeyDisplay convention) — one key-color
  scheme app-wide.
- BPM delta bands: ≤2% green · ≤4% yellow · ≤8% orange (#ff9500 — the
  `--peach` value; the var is scoped to `.set-header`) · beyond red.
  Riding's FIRST row has no predecessor → neutral (undecided in triage;
  flag if you want something else).
- BPM text/delta read `bpm_effective` (ADR 0016) — the old row showed raw
  `bpm`; the plan-authoritative value is the honest one to color.
- NEVER AUDIBLE: badge unchanged; both time cells blank (an entry that
  never enters has no honest "in" time).
- Known judgement call for review: `fixedTempoRef` re-states the planner's
  unset-Set-tempo fallback (first track's effective BPM) rather than
  reading it off the plan — the planner doesn't export its resolved tempo,
  and planner.ts is contested (setlist lane, 24) so it wasn't touched.

## Verification walkthrough (ready-for-human)

Lane app on setui (ports +140): open http://localhost:5313 (or
`npm --prefix desktop start -- --port 5313`).

1. Sidebar → Sets → open the demo Set. Rows now read
   ▶ · # · key · BPM · title/artist · … · in · play · ✕ — key/BPM and the
   two time columns align as columns down the list.
2. Key cells: Camelot-hue identity colors (same hues as the library's key
   column). BPM cells: absolute value, colored by delta — hover one for
   the tooltip ("+x.x% vs …").
3. Click the tempo-policy chip (header): switch Riding ↔ Fixed. Under
   Fixed every BPM colors against the Set tempo; under Riding against the
   row's predecessor (first row neutral grey).
4. Time columns: "in" = mix-clock entry instant; "play" = audible span /
   track length (hover for tooltips). A NEVER AUDIBLE row keeps its badge,
   time cells blank.
5. Adjacency rows: chips still start exactly at the title x (now driven by
   the shared constants); drag a row — the live preview keeps every column
   aligned and BPM colors follow the hypothetical order.
6. Selection ring / conducting wash / hover / badges: unchanged, legible
   over the new columns.
