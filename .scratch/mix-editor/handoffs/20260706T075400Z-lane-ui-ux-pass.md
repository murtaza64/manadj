# Handoff: mix-editor issue 32 — lane UI/UX pass

## Task

Implement `.scratch/mix-editor/issues/32-lane-ui-ux-pass.md`. The issue is
the full spec — ten independently-verifiable items settled in a 2026-07-05
grill (per-deck lane toggles, deck-family lane colors, zoned cue marks,
TEMPO/SNAP button toggles, flat strips, terse labels, lock-on default,
slide polarity flip, padding, vertical overflow). File:line pointers for
every touch point are in the issue body.

## Context

- PRD: `.scratch/mix-editor/PRD.md`; iteration notes `.scratch/mix-editor/NOTES.md`.
- Glossary already updated (land `lkvmwmor`): CONTEXT.md "Slide" carries
  the new arrow-polarity convention (jogs exempt — platter metaphor);
  "Deck color" carries the lane hue-family rule. The implementation must
  match those entries; don't re-decide.
- Main surfaces: `frontend/src/editor/` — `TransitionEditor.tsx`,
  `DawTimeline.tsx`, `LaneCanvas.tsx`, `laneColors.ts`, `editorStore.ts`,
  `transitionEditor.css`, `GlobalMinimap.tsx`, `DeckCard.tsx`; plus
  `WaveformRendererV2.ts` for the zoned cue marks (item 3).
- Item 3 caution: `WaveformRendererV2` is shared with Performance view.
  Only the editor-row triangle path changes (triangles → square flags);
  the performance minimap zoned-mark path is the *reference*, not a
  target. `HOT_CUE_CSS_COLORS` in the same file is the fallback palette.
- Item 8 caution: flip signs at the gesture layer only (DeckCard buttons,
  MIDI beatjump port, alignment nudges). `slideB`/`mixModel` math is NOT
  wrong — do not flip it there or hot-cue slides/jog break. Jog B keeps
  current polarity. Reword tooltips.
- Item 4: the top-bar quantize toggle (`components/TopBar.css`) is the
  visual model for TEMPO/SNAP. Active fill = `var(--green)`, never
  sapphire; blur after click (see `.scratch/keyboard-focus/issues/01`).

## Coordination

- Issue 16 (lane node group selection) is ready-for-human on lane
  `lanesel`, change `rlrspylz` — NOT landed. Issue 30 (chop swap stamp)
  is ready-for-agent, may be in flight. All three touch
  LaneCanvas/DawTimeline. Check `jj log` and `.lanes/` before starting;
  if 16 lands mid-work, merge trunk into your lane at an item boundary.

## Ground rules

- Fresh lane per `docs/agents/parallel-work.md` (ADR 0012). Feature work:
  review-gated — when done, run the lane app
  (`uv run scripts/agent/lane_app.py start`), flip the issue to
  `ready-for-human` with a verification walkthrough (URL + the clicks per
  item), toast the human, and print the walkthrough in your output. Do
  not advance `main`.
- One jj change, described `mix-editor: 32-lane-ui-ux-pass`.
- Gate: tsc, eslint on touched files, vitest green; pure logic (polarity
  signs, visible-lane math, color table) under vitest; NOTES.md entry.

## Suggested skills

- `implement` — primary flow
- `tdd` — for the pure-module pieces (polarity, lane-set math)
