# Handoff: midi-performance-ops — grid-edit track (issues 04 → 05 → 06)

## Context

The midi-performance-ops design is fully grilled and landed: `.scratch/midi-performance-ops/PRD.md` (ready-for-agent). This track is the Beatgrid-editing third. Glossary on trunk already carries the Grow/Shrink term (CONTEXT.md) — read it, plus the Beatgrid and Nudge entries (grid nudge ≠ performance Nudge ≠ Grow/Shrink).

## Task

Implement, in order, as separate changes (one jj change per issue, description `midi-performance-ops: <issue-file-stem>`):

1. `.scratch/midi-performance-ops/issues/04-nudge-offset-param.md` — backend nudge endpoint gains an explicit ms offset
2. `.scratch/midi-performance-ops/issues/05-grid-edit-pad-mode.md` — SAMPLER pads become grid editing (tap layer) + lamps + grow/shrink code rename
3. `.scratch/midi-performance-ops/issues/06-spin-to-nudge-chord.md` — the chord reducer (the feature's one NEW tested seam), jog interception, commit-on-release

## Workspace

Open a fresh lane per `docs/agents/parallel-work.md` (suggest name `midigrid`, `jj workspace add --name midigrid -r main ../manadj-midigrid`; create `.lanes/midigrid.md`, pick an unclaimed port offset — check `.lanes/`). Issue 04/05 touch the backend: clone the sandbox DB (`cp -c /Users/murtaza/manadj/data/library.db data/library.db`).

## Coordination (read before touching shared files)

Two sibling lanes work the same feature concurrently (loops track: issues 01-03, in lane `midiops`; toggles track: 07-08). Shared hotspots, additive-append style (new entries at the END of lists/sections, never reshape): `frontend/src/midi/actions.ts`, `mappings/inpulse300mk2.ts`, `dispatch.ts`, `controlRegistry.ts`, LED derivation files. The grow/shrink rename in issue 05 touches `BpmControl`/deck-controls naming — that is NOT a shared hotspot, but keep it inside its own change. Record claims in your `.lanes/` file; land per issue, promptly.

## Key design facts not in the issue files

- SAMPLER pad addresses per Mixxx's `Hercules_DJControl_Inpulse_300.midi.xml`: notes 0x30–0x37 base on pad channels 6/7, lamps at the same addresses. No device access: bind with `TODO(hardware-verify)` comments, matching the existing mapping file's discipline.
- Grid targets are registry-direct (never surface-routed) per ADR 0019 (`docs/adr/0019-...`); issue 06's jog interception is a deliberate carve-out — add the amendment note to that ADR in the same change.
- The chord reducer is modeled on the transport-reducer tests (see `frontend/src/playback/`); dispatch already tracks per-port decoder state but has NO chord state today — the reducer is where it lives.
- Fine paused seeks already use the jog touch stream (CC 0x0A); spin-to-nudge listens to both rim and touch while armed.

## Landing policy

Feature work = review-gated. Verify per the issues' acceptance criteria + PRD Testing Decisions (`uv run -m pytest` for backend, `npx vitest run` + `npm --prefix frontend run build` for frontend), then park ready-for-human with a verification walkthrough per `docs/agents/parallel-work.md` — do NOT advance `main` yourself except the docs fast-path for status flips. Alembic: no schema change expected; if you add one anyway, single-head invariant applies.

## Suggested skills

- `implement` (primary loop)
- `tdd` (the chord reducer seam and backend router tests are test-first naturals)
