# Handoff: midi-performance-ops — loops track (issues 01 → 02 → 03)

## Context

The midi-performance-ops design is fully grilled and landed: `.scratch/midi-performance-ops/PRD.md` (ready-for-agent). This track is the loop third of it. Glossary is already updated on trunk (CONTEXT.md: Active loop rewritten for the dyadic domain; Grow/Shrink added) — read the Active loop entry before starting.

## Task

Implement, in order, as separate changes (one jj change per issue, description `midi-performance-ops: <issue-file-stem>`):

1. `.scratch/midi-performance-ops/issues/01-dyadic-loop-domain.md` — loop max 32→128, set-length resize generalization
2. `.scratch/midi-performance-ops/issues/02-loop-pad-mode.md` — LOOP pad mode bindings, engage/release/resize, lamps
3. `.scratch/midi-performance-ops/issues/03-shift-in-out-overload.md` — SHIFT+IN/OUT state-disambiguated overload

## Workspace

You are being handed the existing quiescent lane `midiops` (`/Users/murtaza/manadj-midiops`, registry `.lanes/midiops.md`, port offset +210). Update the registry's claimed areas when you start; clone the sandbox DB only if you need one (`cp -c /Users/murtaza/manadj/data/library.db data/library.db` — likely unnecessary for this track).

## Coordination (read before touching shared files)

Two sibling lanes work the same feature concurrently (grid track: issues 04-06; toggles track: 07-08). Shared hotspots, additive-append style per `docs/agents/parallel-work.md` (new entries at the END of lists/sections, never reshape):

- `frontend/src/midi/actions.ts` (closed action vocabulary — append new targets)
- `frontend/src/midi/mappings/inpulse300mk2.ts` (append binding sections)
- `frontend/src/midi/dispatch.ts`, `controlRegistry.ts`
- LED: `frontend/src/midi/feedback.ts`, `outputStore.ts`, mapping `feedback` block (toggles track also adds lamps)

Record claims in `.lanes/midiops.md`. Expect merge-shaped landing; land per issue, promptly.

## Key design facts not in the issue files

- Hardware addresses come from Mixxx's `Hercules_DJControl_Inpulse_300.midi.xml` (LOOP pads: notes 0x10–0x17 base / 0x18–0x1F shifted on pad channels 6/7; lamp addresses match). No device access in your session: bind with `TODO(hardware-verify)` comments, exactly like the existing mapping file's discipline.
- Loop gestures route through the existing loops gesture class / audible-surface arbiter (ADR 0019); pad targets carry the preset size.

## Landing policy

Feature work = review-gated. Verify per the issues' acceptance criteria + PRD Testing Decisions (vitest pure seams; `npx vitest run` in `frontend/`, `npm --prefix frontend run build`), then park ready-for-human with a verification walkthrough per `docs/agents/parallel-work.md` — do NOT advance `main` yourself except the docs fast-path for status flips.

## Suggested skills

- `implement` (primary loop)
- `tdd` (loop-math and translator seams are pure and test-first friendly)
