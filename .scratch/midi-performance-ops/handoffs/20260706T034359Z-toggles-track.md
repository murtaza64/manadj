# Handoff: midi-performance-ops — toggles track (issues 07 + 08)

## Context

The midi-performance-ops design is fully grilled and landed: `.scratch/midi-performance-ops/PRD.md` (ready-for-agent). This track is the small third: Quantize/Key Lock buttons and the assistant Follow macro. Read CONTEXT.md's Quantize, Key Lock, and Follow mode entries first — the button semantics lean on their exact definitions.

## Task

Implement, in either order, as separate changes (one jj change per issue, description `midi-performance-ops: <issue-file-stem>`):

1. `.scratch/midi-performance-ops/issues/07-quantize-keylock-buttons.md` — Q toggles app-wide Quantize (both lamps mirror), SHIFT+Q per-Deck Key Lock, shifted-Q lamp probe note
2. `.scratch/midi-performance-ops/issues/08-assistant-follow-macro.md` — assistant button as a pure macro over per-Deck Follow, lamp lit iff any Deck follows

## Workspace

Open a fresh lane per `docs/agents/parallel-work.md` (suggest name `miditog`, `jj workspace add --name miditog -r main ../manadj-miditog`; create `.lanes/miditog.md`, pick an unclaimed port offset — check `.lanes/`). Frontend-only; no DB clone needed.

## Coordination (read before touching shared files)

Two sibling lanes work the same feature concurrently (loops track: issues 01-03, in lane `midiops`; grid track: 04-06). Shared hotspots, additive-append style (new entries at the END of lists/sections, never reshape): `frontend/src/midi/actions.ts`, `mappings/inpulse300mk2.ts`, `dispatch.ts`, `controlRegistry.ts`, and especially the LED files (`feedback.ts`, `outputStore.ts`, the mapping's `feedback` block) which the loops track also extends. Record claims in your `.lanes/` file; land per issue, promptly.

## Key design facts not in the issue files

- Q button messages need learning; Mixxx's `Hercules_DJControl_Inpulse_300.midi.xml` shows Q at note 0x02 on the deck transport channels (input AND lamp), shifted layer on channel+3. The assistant button's message is unknown — infer nothing; bind with `TODO(hardware-verify)` placeholders clearly marked, or leave the matcher values as documented TODOs for the human's inspector pass. Existing discipline: every unverified address carries `TODO(hardware-verify)`.
- Mixxx drives NO output at the shifted-Q address but does drive other shifted-layer lamps — hence the probe framing in issue 07: wire the lamp optimistically behind the probe TODO, with a comment explaining both outcomes.
- All targets here are registry-direct per ADR 0019 (sticky toggles / browse-adjacent), never surface-routed.
- The follow macro decision function belongs beside the existing follow reducer (`frontend/src/follow/model.ts` area) — pure: follow states + playing states in, per-deck toggles out. The per-Deck model, spread/revoke bridge, and FilterBar stay untouched.
- Quantize state: `frontend/src/playback/quantizeStore.ts` (app-wide, sticky); Key Lock: `frontend/src/playback/keyLockStore.ts` + deck engine setter.

## Landing policy

Feature work = review-gated. Verify per the issues' acceptance criteria + PRD Testing Decisions (`npx vitest run` + `npm --prefix frontend run build`), then park ready-for-human with a verification walkthrough per `docs/agents/parallel-work.md` — do NOT advance `main` yourself except the docs fast-path for status flips.

## Suggested skills

- `implement` (primary loop)
- `tdd` (macro function and LED derivations are pure seams)
