/**
 * Hook for accessing audio playback state and controls.
 *
 * This is a convenience re-export of useAudioContext from AudioContext.
 * Use this hook in components that need to interact with the audio player.
 */

export { useAudioContext as useAudio } from '../contexts/AudioContext';
