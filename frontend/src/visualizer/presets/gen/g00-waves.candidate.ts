/** gen-0 seed: curated waves enters the pool as a rated baseline. */
import { wavesPreset } from '../waves';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...wavesPreset, name: 'g00 waves' };
export default candidate;
