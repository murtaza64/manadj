/** gen-0 seed: curated pulse enters the pool as a rated baseline. */
import { pulsePreset } from '../pulse';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...pulsePreset, name: 'g00 pulse' };
export default candidate;
