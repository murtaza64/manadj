/** gen-0 seed: curated fracture enters the pool as a rated baseline. */
import { fracturePreset } from '../fracture';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...fracturePreset, name: 'g00 fracture' };
export default candidate;
