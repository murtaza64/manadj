/** gen-1 seed: Odyssey (phrase-genome Voyage evolution, human-built in
 * this lane) enters the pool as breeding stock. */
import { odysseyPreset } from '../odyssey';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...odysseyPreset, name: 'g01 Odyssey' };
export default candidate;
