/** gen-0 seed: curated bars enters the pool as a rated baseline. */
import { barsPreset } from '../bars';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...barsPreset, name: 'g00 bars' };
export default candidate;
