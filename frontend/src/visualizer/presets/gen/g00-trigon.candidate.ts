/** gen-0 seed: curated trigon enters the pool as a rated baseline. */
import { trigonPreset } from '../trigon';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...trigonPreset, name: 'g00 trigon' };
export default candidate;
