/** gen-0 seed: curated plasma enters the pool as a rated baseline. */
import { plasmaPreset } from '../plasma';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...plasmaPreset, name: 'g00 plasma' };
export default candidate;
