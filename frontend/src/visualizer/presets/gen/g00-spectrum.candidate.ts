/** gen-0 seed: curated spectrum enters the pool as a rated baseline. */
import { spectrumPreset } from '../spectrum';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...spectrumPreset, name: 'g00 spectrum' };
export default candidate;
