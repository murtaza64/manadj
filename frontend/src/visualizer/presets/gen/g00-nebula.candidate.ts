/** gen-0 seed: curated nebula enters the pool as a rated baseline. */
import { nebulaPreset } from '../nebula';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...nebulaPreset, name: 'g00 nebula' };
export default candidate;
