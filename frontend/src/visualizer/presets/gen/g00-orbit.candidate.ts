/** gen-0 seed: curated orbit enters the pool as a rated baseline. */
import { orbitPreset } from '../orbit';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...orbitPreset, name: 'g00 orbit' };
export default candidate;
