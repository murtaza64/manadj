Candidate: g19-strata-engine   Kind: novel
Parents: none; gen-19 free ideation (nested-timescale abstract geometry —
the accretion direction). Raids the g13-truchet quantized-grammar skeleton
(pseudo-meter, latched impulses) but shares no visual vocabulary.
Human notes in play: "focus on meter and long evolution... more immediate
contrast between sections and longer periods... abstract geometric ones."
Question: Can a structure that visibly ACCRETES — bar = one element,
section = one stratum, epoch = one architecture — make minute-10 read as
the payoff of minute-1, while every 16-bar boundary still slams?
Instruction: A radial architecture builds outward from the center over a
128-bar epoch: 8 concentric strata (one per 16-bar section) x 16 elements
(one per bar). The current bar's element assembles in FOUR QUANTIZED
quarter-steps, one per beat. Each stratum has its own geometric vocabulary
(spokes / chords / shell arcs / zigzag / ticks / squares / triangles /
gates), genome-shuffled per epoch — a section boundary opens a NEW ring
with a NEW vocabulary and a hue slam while the finished stratum snap-dims.
EVEN epochs accrete outward; ODD epochs DISASSEMBLE inward one element per
bar with a shifted hue family — then the next epoch rebuilds a different
architecture (fresh vocabulary shuffle). Fill fraction of the ring = your
position in the epoch at a glance.
Invariants: element count = bar-in-epoch exactly (stateless from the
ladder, robust to bar jumps); quantized assembly only (no eased growth);
dark floor, stroke-built flat geometry, no dust, no feedback buffer.
Degrees of freedom: vocabulary details, stratum radius bands, disassembly
reading (removal vs collapse).
Assigned tech: beat.barPhase quarters (assembly), ladderBarIndex tiers
(bar/section/epoch), impulse.low kick pump (solid scale), impulse.mid
stratum-outline flash, impulse.high newest-element glints, bandsSlow
weights, trackId genome via dominantChannel, pseudo-meter fallback.
Anti-resemblance: NOT an orrery (no gears/orbiting bodies), NOT the advent
calendar grid, NOT tally-foundry counters, NOT truchet tiles.
Contract: default-export VisualizerPreset; canvas 2D; frame fields:
bands/impulse/beat/decks/params/time/dt.
Hard rules: self-contained file; bright saturated colors; photosensitivity
floor; motion terms ride bandsSlow.
