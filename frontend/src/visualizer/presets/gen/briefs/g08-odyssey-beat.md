# g08-odyssey-beat

Candidate: g08-odyssey-beat   Kind: tweak (beat-grammar + alt-mid)
Parents: g01-odyssey (engine presets/odyssey.ts — gen file is a
re-export).
Human ask: odyssey variants with more beat-based effects + alternative
mid/high responses.
Instruction: copy the odyssey engine (keep phrase theatre + section
transformations). Beat grammar: the scene holds POSES that re-strike
per beat — camera orientation micro-steps (small quantized rotation
increments on each beat, no continuous drift between), and the warp
field pulses in beat-locked breaths. ALTERNATIVE MID response: mids
drive warp-field TOPOLOGY (churn scale/fold count) instead of dust
density. ALTERNATIVE HIGH response: highs = sparkle running along the
charged horizon ring + rim light on structures (no powder). Kick =
parent ripple + pose strike emphasis. Drop = poses release into
continuous motion on max(drop, energy) (quantized->fluid contrast, same
trick as the escapement clock).
Standing law: docs/visualizer-ga.md (taste calibration, hard safety, dust fatigue). Focus (human): EQ + beat responsiveness, dramatic kicks/drops, contrast, movement. Hard cuts/steps land exactly on grid via `beat.ladderBarIndex ?? beat.barIndex` + beat phase; integer things never interpolate.
File: g08-odyssey-beat.candidate.ts.
