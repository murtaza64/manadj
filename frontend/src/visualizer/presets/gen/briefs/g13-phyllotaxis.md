Candidate: g13-phyllotaxis   Kind: novel
Parents: none (fresh territory — golden-angle / Vogel spiral point lattice)

Territory: ABSTRACT MATHEMATICS & GEOMETRY not yet tried. This is a
PHYLLOTAXIS seed-head: florets placed by Vogel's model — the nth floret at
angle n·137.507° (the golden angle) and radius sqrt(n)·spacing. A sunflower
head / pinecone lattice, NOT a spiral field. Points are DISCRETE flat matte
polygons on a dark floor; there is no continuous rotating hypno texture and
no glow. Anti-resemblance clearance: hypno spirals are analytic band arms
that rotate — this is a fixed-count discrete point set whose COUNT grows
with the music and whose per-floret color is banded by ring index. Color
organ ribbons / stained glass / mirror strata are all different families.

Falsifiable question: does a DISCRETE golden-angle point lattice, grown to
the beat with hard integer count-jumps and banded ring vocabularies, read as
legible botanical mathematics rather than a wash?

Invariants:
- FLAT: solid matte florets, hard edges, committed palette, dark floor. No
  feedback buffer, no additive haze, no blur. Canvas 2D, source-over.
- Floret COUNT is quantized: it jumps by an integer step on each beat cell
  and holds between beats (grown, never smoothly interpolated). The visible
  count rides an envelope that snaps up on kicks.
- Motion (head rotation drift, breathing zoom) rides bandsSlow, never
  instantaneous bands (erratic-motion law). Instantaneous bands/impulse
  drive floret brightness pops and count snaps only.
- Legible per-band vocabulary:
    LOW  = the head PULSES outward (spacing/scale) + kicks snap the newest
           outer ring solid-bright for a beat (gated on impulse.low so it is
           not kick-powder).
    MID  = snares rotate the ENTIRE head one hard 137.5°/parastichy step
           (a quantized turn, not a spin) + set floret polygon fill weight.
    HIGH = hats sparkle the OUTERMOST florets (per-floret edge accents on
           the youngest ring only — a rim glint, not dust across the field).
- Ring banding: florets colored by their ring ordinal (floor of sqrt(n)) so
  the eye reads concentric parastichy bands, each a committed scheme color;
  centroid rotates which scheme color leads (shape carries band identity,
  color is free to travel).
- Photosensitivity: no fullscreen flash; all pops are localized florets.

Degrees of freedom (3-5 params): head scale, floret size, growth density
(max floret count multiplier).

Grammar / quantized tiers (beat.ladderBarIndex ?? barIndex; pseudo-meter
fallback when gridless):
- BEAT  = count grows one integer step (florets bloom outward in order).
- BAR   = floret polygon SIDES cycle (5 -> 6 -> 8, the seed-head facet), a
          hard step.
- PHRASE(4 bars) = scheme rotates one color; parastichy handedness may flip.
- SECTION(16 bars) = scheme SWAP + head recenters/relocates (hard cut) +
          growth resets to a seeded floor and re-blooms (theatre).
- DROP  = growth ceiling lifts hard (dense full head) riding max(drop,
          energy); alternate rings read solid vs outline in a CHROMA-only,
          luminance-held, <=2 Hz safe alternation.

Genome: dominant audible deck trackId seeds scheme family, sides-cycle
phase, parastichy handedness, and section relocation orbit — same song, same
head. No trackId => frozen pseudo-seed from centroid/energy/spread.

Assigned tech: bandsSlow (rotation/zoom velocity), per-band impulses
(kick count-snap / snare turn / hat rim glint), trend drop/buildup split
(growth ceiling, chroma alternation), centroid (leading scheme color),
ladder tiers bar/phrase/section, trackId genome.

Contract: default-export VisualizerPreset; frame fields available:
bands/impulse/trend/centroid/spread/flatness/beat/decks/spectrum/wave/
params/time/dt. Self-contained file; bright saturated colors; no protocol
or bridge changes.
