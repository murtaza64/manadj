Candidate: g19-guillotine   Kind: novel
Parents: none; gen-19 free ideation (rotation-free direction — the
strobe-column/hardcut lesson generalized: NOTHING glides, only cuts move).
Human notes in play: "more immediate contrast between sections and longer
periods... abstract geometric ones"; FLAT APPETITE (gen-10: solid matte
fills, hard edges, committed schemes, motion by transforms and swaps).
Question: Can a composition whose ONLY motion is discrete cut moves —
zero rotation, zero translation, zero easing — stay alive at beat rate
while an 8-section density arc gives it a long shape?
Instruction: A guillotine partition of the screen (axis-aligned recursive
splits) painted in a committed flat 5-color scheme with thick dark seams.
BEAT: one existing cut RE-SNAPS to a new eighth-fraction (single-frame).
BAR: one cut is ADDED (largest panel splits) on even epochs / REMOVED
(two panels merge) on odd epochs. PHRASE (4 bars): panel colors re-deal.
SECTION (16 bars): the whole partition REBUILDS at a planned cut budget —
[3,6,9,12,16,20,25,30] across the epoch's 8 sections (reversed on odd
epochs: dense→sparse) — plus a scheme change: the drastic flip. EPOCH
(128 bars): build/dissolve direction inverts and the palette family
steps. Panels carry band identity: each panel is hashed to low/mid/high
and its fill brightness rides that band (EQ-killable) — brightness is the
only continuous life. Kick: panels flanking the last-snapped cut pop.
Snare: one seam flashes thick. Hats: intersection ticks.
Invariants: everything axis-aligned; no continuous spatial motion at all;
fractions quantized to eighths; panel count capped (~48); luminance
parity across schemes; dark seams always visible.
Degrees of freedom: seam weight, scheme families, budget curve.
Assigned tech: ladderBarIndex tiers, per-band impulses (pop/seam/ticks),
bands per-panel brightness, bandsSlow nothing-moves (rotation-free
thesis), trackId genome via dominantChannel (scheme orbit, split rng),
pseudo-meter fallback.
Anti-resemblance: NOT mondrian-via-feedback, NOT flip-matrix split-flaps,
NOT signal-quadrants panels, NOT strata mirror bands.
Contract: default-export VisualizerPreset; canvas 2D; frame fields:
bands/impulse/beat/decks/params/time/dt.
Hard rules: self-contained file; bright saturated colors;
photosensitivity floor (pops are localized, seams thin).
