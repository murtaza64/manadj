# g12-strobe-column

Candidate: g12-strobe-column   Kind: novel (TECHNO WAVE)
Human ask: darker, monochromatic, kinetic.
Concept: a row of massive COLUMNS (8-16, flat, monochrome grays). Each beat, a
genome-sequenced SUBSET of columns inverts state (dark<->lit gray — spatial
alternation keeps total luminance near-constant: photosafe checkerboard logic,
never full-field). Bar position readable: the inversion pattern completes a
cycle each bar. Mids = column HEIGHTS (bandsSlow.mid, slow); highs = thin edge
flicker detail on lit columns (discrete). KICK = the whole colonnade SHIFTS one
column-width sideways (hard transform — the room strides). Buildup = columns
narrow (compression); DROP = double-time inversions (still <=2 state changes/sec
per region via spatial partitioning) + one accent hue floods the lit set, riding
max(drop, energy). Section = column count + gray ramp + accent swap.
Standing law: docs/visualizer-ga.md — taste calibration, hard safety (photosensitivity floor), feedback contraction, MOTION SMOOTHNESS (rates ride frame.bandsSlow ?? frame.bands), luminance-parity palettes. Phrase/section via `beat.ladderBarIndex ?? beat.barIndex`. Dust is BACK by explicit human request — but DIVERSIFIED: distinct species per band, distinct hues, never the old monochrome wash.
File: g12-strobe-column.candidate.ts.
