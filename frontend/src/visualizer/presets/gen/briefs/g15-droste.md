# g15-droste

Candidate: g15-droste   Kind: novel (feedback-space optics lens)
Parents: g00-voyage (unsharp tap, soft knee — optics idioms); g10-poster /
the gen-10 FLAT cohort as a STYLE prior (committed flat schemes, hard
edges) — different mechanism.

Human notes in play: FLAT APPETITE ("solid matte fills, hard edges,
committed 3-5 color schemes, motion by transforms and color swaps");
the aberration fluid ("build ON feedback-space optics"); "pops, flips,
wipes, scheme swaps encouraged".

Falsifiable question: does an OCTAVE-ECHO TAP — a second half-scale
sample of the previous frame blended into the resample, so every
structure spawns a shrunken self-copy at the center that then grows —
turn bold flat geometric stamps into an infinite poster tunnel (Droste
cascade) that reads as flat design in motion rather than glowy feedback
soup?

Instruction: infinite poster tunnel. The feedback tap is dual-scale:
main tap slowly zooms IN (content marches outward, rate on bandsSlow +
regime.sustained; buildups reverse to a gentle inward collapse) with a
genome twist per octave (log-spiral Droste); an ECHO tap samples the
frame at 2x coordinates (a half-size copy) and is blended in near the
center, gated by mids — so every kick ring becomes rings-of-rings
cascading outward bar after bar. Stamps are FLAT and OPAQUE (convex mix,
not additive glow): KICK = thick hard-edged ring in the scheme's punch
color; SNARE = an n-gon outline (n and orientation hashed per hit);
BAR boundary = a sector wipe that repaints an advancing angular slice in
the next scheme color (luminance-matched — photosafe); SECTION (16-bar
ladder) = full scheme rotation from the trackId genome (dominantChannel
law). flatness → edge softness (tonal = razor edges, noisy = soft);
spread → twist magnitude. Palette: committed 4-color flat scheme (near-
black ground + three saturated inks) derived from the genome hue.

Invariants: contraction (echo blend is convex; decay < 1; grade cap
0.99; soft knee); zoom/twist rates on bandsSlow (motion law); echo tap
gated to the central region where 2x coords stay in-texture; sector
wipes ≤ 1/6 field per bar with matched luminance (photosafety).

Degrees of freedom (params): persistence, march speed, echo strength.

Assigned tech: beat phase + bpm + bar/phrase/section ladder tiers,
trackId genome via dominantChannel, spectral flatness + spread, regime,
bandsSlow, per-band impulses.

Anti-resemblance: not tunnel (raymarched glow corridor — this is flat
ink and transforms); not CRT (no scanlines); not hypno (no continuous
spiral field — discrete cascading copies); no dust.

Contract: default-export VisualizerPreset; GL via createGlRenderer
(context-loss safe); GLSL ES 1.0, no backticks in GLSL.
File: g15-droste.candidate.ts.
