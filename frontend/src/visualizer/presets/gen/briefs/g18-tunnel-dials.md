Candidate: g18-tunnel-dials   Kind: combine
Parents: g14-tunnel-verses (quantized mouth cuts × saga chapter arc); g08-voyage-beatgate (meter-readable structure: segmented charging ring, runway sector)
Human notes in play: "more focus on meter"; handoff direction "meter-readable rings/runways"; "bar position should be READABLE".
Falsifiable question: can the tunnel carry a full nested meter READOUT — beat pips inside the mouth, bar arcs ON the mouth, phrase diamonds outside, plus a wall runway sector advancing per beat — so that beat-in-bar, bar-in-phrase AND phrase-in-section are all literally readable at a glance, without the instrumentation killing the ride?
Invariants: three nested dials, one per tier — INNER: beatsPerBar pips, elapsed beats filled, current pip fades in over its beat (photosafe), full on the downbeat then reset; MOUTH: the mouth ring split into 4 bar arcs with gaps, elapsed bars lit, the current bar's arc SWEEPS with barPhase like a clock hand; OUTER: 4 phrase diamonds, one lights per phrase of the section, all reset on the section cut. Wall runway: one angular wedge of the tunnel wall softly lit, advancing one sector per beat (quantized snap, stamped into the feedback so it trails). Verses' look-cut grammar kept verbatim (phrase cuts, section strides with forced bank change, drop burst, anticipation tighten). Sparkles replaced by the runway (no new dust media).
Degrees of freedom: dial radii/sizes, gap widths, runway wedge width and brightness, sweep easing.
Assigned tech: beat.beatInBar + beat.barPhase + beat.beatsPerBar (dials), beat.ladderBarIndex ?? barIndex (tiers), trackId genome via frame.dominantChannel, bandsSlow motion, per-band impulses (kick = mouth lunge stays instantaneous).
Anti-resemblance: no approaching gates (g18-tunnel-gates), no extinguish-countdown (g18-voyage-countdown), no disk sector gating on a voyage engine (g18-voyage-cascade); this one is the INSTRUMENT-PANEL reading of the meter.
Contract: default-export VisualizerPreset; self-contained Canvas 2D feedback renderer (verses engine, fullbleed cover-factor floor kept); whites capped (~72% lightness); committed luminance-comparable bank hues; bright saturated colors; motion rates on frame.bandsSlow ?? frame.bands; ≤3 full-field flashes/s.
