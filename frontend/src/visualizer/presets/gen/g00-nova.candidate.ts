/** gen-0 seed: curated Nova as a baseline parent. */
import { novaPreset } from '../nova';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...novaPreset, name: 'g00 Nova' };
export default candidate;
