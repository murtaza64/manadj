/** gen-0 seed: curated gonio enters the pool as a rated baseline. */
import { gonioPreset } from '../gonio';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...gonioPreset, name: 'g00 gonio' };
export default candidate;
