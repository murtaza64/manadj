/** gen-0 seed: curated scope enters the pool as a rated baseline. */
import { scopePreset } from '../scope';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...scopePreset, name: 'g00 scope' };
export default candidate;
