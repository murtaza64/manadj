/** gen-0 seed: curated Tunnel as a baseline parent. */
import { tunnelPreset } from '../tunnel';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...tunnelPreset, name: 'g00 Tunnel' };
export default candidate;
