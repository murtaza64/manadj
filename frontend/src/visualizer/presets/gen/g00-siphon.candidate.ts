/** gen-0 seed: curated siphon enters the pool as a rated baseline. */
import { siphonPreset } from '../siphon';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...siphonPreset, name: 'g00 siphon' };
export default candidate;
