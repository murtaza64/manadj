/** gen-0 seed: curated mirror enters the pool as a rated baseline. */
import { mirrorPreset } from '../mirror';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...mirrorPreset, name: 'g00 mirror' };
export default candidate;
