/** gen-0 seed: curated radial enters the pool as a rated baseline. */
import { radialPreset } from '../radial';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...radialPreset, name: 'g00 radial' };
export default candidate;
