/** gen-0 seed: curated silk enters the pool as a rated baseline. */
import { silkPreset } from '../silk';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...silkPreset, name: 'g00 silk' };
export default candidate;
