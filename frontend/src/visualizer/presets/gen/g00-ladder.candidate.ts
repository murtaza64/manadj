/** gen-0 seed: curated ladder enters the pool as a rated baseline. */
import { ladderPreset } from '../ladder';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...ladderPreset, name: 'g00 ladder' };
export default candidate;
