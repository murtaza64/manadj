/** gen-0 seed: curated strata enters the pool as a rated baseline. */
import { strataPreset } from '../strata';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...strataPreset, name: 'g00 strata' };
export default candidate;
