/** gen-0 seed: curated quad enters the pool as a rated baseline. */
import { quadPreset } from '../quad';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...quadPreset, name: 'g00 quad' };
export default candidate;
