/** gen-0 seed: curated aurora enters the pool as a rated baseline. */
import { auroraPreset } from '../aurora';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...auroraPreset, name: 'g00 aurora' };
export default candidate;
