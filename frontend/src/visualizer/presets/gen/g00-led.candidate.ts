/** gen-0 seed: curated led enters the pool as a rated baseline. */
import { ledPreset } from '../led';
import type { VisualizerPreset } from '../types';

const candidate: VisualizerPreset = { ...ledPreset, name: 'g00 led' };
export default candidate;
